---
description: Roll up completed sub-phase results, clear context, prep next sub-phase
argument-hint: [next sub-phase, e.g., 8B]
---
The current sub-phase can now be marked verified and 100% complete. 

Roll all sub-phase execution results and learnings into the "Phase Results and Final Context" section of the phase_[X]_implementation_plan.md. Update the sub-phase status in the sub-phase table to [Tested]. Ensure to note anything that we encountered out of scope and decided not to proceed, as well as any testing or components of this sub-phase that was not completed fully in the implementation plan. 

Then, clear the context window completely — you're starting fresh for the next sub-phase with no prior conversation history.

Once context is cleared, generate the detailed execution section for sub-phase $ARGUMENTS (maintaining the same master_blueprint.md and phase plan artifacts, but focusing exclusively on $ARGUMENTS going forward). Include a brief handoff note from the prior sub-phase at the top of the $ARGUMENTS section so you understand what state was left behind.
