* # Master Blueprint: Base44 Deprecation

  **Status:** APPROVED — Phases 1 through 13 are all Complete (code-complete and functionally delivered; see each phase's own row in Section 4 for detail). **All outstanding/deferred live-testing work across every phase is now tracked centrally in `blueprint_verification_plan.md`** (a living document) rather than duplicated or left open here — treat that document as authoritative for current testing status. The standalone tracking phase "Phase 10A" (Full Inventory Flow + Appointment — Combined Testing & Cleanup) was retired 2026-08-05 without ever being executed as its own phase; its full scope is absorbed into `blueprint_verification_plan.md` Section 3. **Phase 14 (Setup, Admin, Lankar Import) is now substantively complete (2026-08-07)** — 14A-14G all code-complete and live-verified on dev, with 2 narrow items genuinely deferred (not testing-scope choices): 14F's full legacy-work-order upload→extract→create flow needs a real PDF; the 4 dev-tested 14C Setup managers haven't had independent production-UI re-confirmation (their production schema/RLS is already confirmed correct). **Phase 15 (Final Sunset)** is now added below as the next and final phase — the actual `@base44/sdk`/`base44-proxy`/`base44/` tree removal, gated on a separate final-validation/blueprint-verify pass.
  **Prepared:** 2026-08-02 (v1), revised 2026-08-03 (v2, v3, v4, v5, v6, v7, v8), 2026-08-05 (v9 — Phase 10A retired/folded into `blueprint_verification_plan.md`, all phase statuses reconciled), 2026-08-07 (v10 — rolled up a standalone deferred-issues bug-fix session against Phase 5 (AR interest/statement) and Phase 7 (Inventory part-lookup/tag-alongs/OCR-bucket/`is_existing`); no new formal phase, appended notes only), 2026-08-07 (v11 — Phase 14 rolled up to substantively complete following its 14G verification pass; Phase 15 added to Sections 4/5 as the final phase)
  **Baseline commit:** `4ba05162` (development branch)

  > **Numbering note:** Phase numbering restarts at **Phase 1** for this initiative (prior Base44-adjacent work by earlier sessions was informal/untracked, not a labeled "Phase 1/2"). Sub-phase numbers (`1.1`, `1.2`, ...) are reserved for when an individual phase gets its own detailed phase plan. **v4 inserts a new Phase 2 and renumbers everything after it (old 2→3, 3→4, ... 13→14) — if you're cross-referencing an earlier conversation against this doc, phase numbers above 1 shifted by one.**

  ---

  ## 0) Open Questions, Info Requirements & Suggestions

  1. **Resolved — dev environment approach, entity status, Appointment plan, master_context.md permission, Kanban board.** See v3's history in git/prior conversation — all resolved and folded into the phases below.

  2. **New (v4), resolved — PartsTech/Online Ordering is a confirmed failed experiment.** You want it removed entirely, including the `PartsTechCart` table. Live validation (via Supabase CLI during Phase 1 prep) confirmed that table's migration was **never applied to production** — so there's no live table to drop, which simplifies this considerably. New Phase 2 below scopes the full removal.

  3. **New (v4), resolved — WorkOrderForm duplicate is already gone.** You flagged two `WorkOrderForm.jsx` files (one in `form/`, one at `work-orders/` root) as a problem. I checked git history: the root-level duplicate was already deleted in the most recent commit on this branch (`019a26c2`, "Online Ordering Integrations"). **No cleanup action needed** — only `src/components/work-orders/form/WorkOrderForm.jsx` exists now, and it's the one actually used by `DocumentEditor.jsx`. Logged in Lessons Learned (Section 7) so this doesn't get re-flagged later.

  4. **✅ Resolved.** `PARTSTECH_API_KEY` was unset from production by the user directly during Phase 2 execution. `PartsTechCart` was also dropped from production and the `partstech-extension.zip` deleted from Storage, all by the user. Phase 2 is fully complete and Tested.

  5b. **New (Phase 6 planning), resolved — deferred a small piece of Technician Performance Report to Phase 10.** `getTechnicianPerformanceReport`'s "Monthly Payroll Target vs Labour Sales" progress bar reads `CashFlowSummary` (confirmed no native table exists yet — bundled into Phase 10). Phase 6 migrates the report's utilization/efficiency data (fully native already) and hardcodes the progress bar's `target`/`current` to 0, hiding that card client-side until Phase 10 lands. **Action item for whoever scopes Phase 10's detailed plan:** restore the payroll-target progress bar in `TechnicianPerformanceReportModal.jsx` (unhide the card) and its backing logic in `autopro-getTechnicianPerformanceReport` (see the `TODO(Phase 10)` comment left in that function, referencing `base44/functions/getTechnicianPerformanceReport/entry.ts` lines 396–420 for the original calc) once `CashFlowSummary` has a native table. Verification of this specific card was explicitly skipped in Phase 6 since it's intentionally non-functional until then.

  5. **New — security finding, needs your call on timing/approach.** While restoring Phase 1's dev environment (see `phase_1_dev_environment_parity_plan.md`), found that two production triggers (`sync_customer_to_google` on `Customer`, `WorkOrder_Broadcast` on `WorkOrder`) have a **live service-role/anon JWT hardcoded in plaintext directly in the trigger definition**, visible to anyone with schema read access — not just in `.env`. Also found the `Google-Contacts-Sync` edge function that one of them calls isn't tracked in your local repo's `supabase/functions/` at all — it's a live, untracked deployment. Neither is blocking any current phase, but worth a decision: rotate the embedded token and move to a vault-based secret reference, and either pull `Google-Contacts-Sync`'s source into the repo or confirm it's intentionally managed outside git. Not scoped into any phase yet — let me know if you want it folded into Phase 14 (final sunset/cleanup) or handled as its own quick fix.

  ---

  ## Practical answer: Phase 1 — dev branch schema/function sync + Vercel secrets

  See `Plans and Context/phase_1_dev_environment_parity_plan.md` for the full, validated, step-by-step execution plan — that document is the source of truth for Phase 1 (this section stays as a short pointer so the blueprint doesn't drift out of sync with it). **Note:** that plan's original "Step 0" (the dangling `PartsTechCart` migration) is now resolved by Phase 2 below — since the feature is being deleted outright, the fix is deleting the migration file, not repairing/applying it.

  ---

  ## 1) Objectives

  **TL;DR:** AutoPRO still round-trips through the legacy Base44 SaaS platform for a large share of its backend calls (279 `base44.functions.invoke` sites, 122 of them the generic `SupabaseProxy` CRUD shim, across 129 files), even though a good portion of the underlying data already lives in your own Supabase Postgres project. The goal is to finish cutting every remaining thread back to Base44 — function calls, entity CRUD, auth, file/OCR integrations — module by module, then delete the Base44 SDK, the `base44-proxy` bridge function, and the legacy `base44/` function tree. **(v4 addition: also remove the unrelated-but-adjacent PartsTech/Online Ordering feature, a confirmed failed experiment, early — before it can cause confusing test failures in later phases.)**

  **What's actually left, now that entity status is fully confirmed:**
  - **Fully native already** (transport-layer cleanup only): Customer, Vehicle, GLTransaction, all WorkPRO tables, `BankAccount`, `BankTransaction`, `BankReconciliation`, `CashDrawerAdjustment`, `DepositSlipBreakdown` (all 5 confirmed fully migrated in Phase 8, 2026-08-03 — see below).
  - **Hybrid** (table + data migrated, CRUD not fully cut over): Appointment, ChartOfAccount, InventoryReturn, FiscalPeriod.
  - **Confirmed still Base44-only** (real schema design + data migration required, not just a rewire): `CashFlowEntry`, `CashFlowSummary`, `GSTReturn`, `LinesOfCredit`, `LinesOfCreditTransaction`, `SystemSettings`, `PayrollTransaction`, `Levies`, `OtherChargeList`, `TagAlong`, `WorkOrderStatus`, `Statement`, `SentEmailLog`, `RealtimeSignal`, `CustomerPortalWorkOrder` — **`InventoryCategory`, `InventoryLocation`, and `ReturnReason` migrated in Phase 7**, **`CashDrawerAdjustment`/`DepositSlipBreakdown` migrated in Phase 8** (new native tables + full CRUD cutover; see `phase_7_implementation_plan.md` Section 7A / `phase_8_implementation_plan.md` Section 8A).
  - **Update (2026-08-03, Phase 13 execution):** `SystemSettings`, `WorkOrderStatus`, `TagAlong`, and `OtherChargeList` are no longer Base44-only — the user created all four as native Postgres tables on the **dev branch** mid-Phase-13 (RLS was enabled with zero policies on all four, the same landmine as Phase 1's finding above; fixed with the standard permissive policy before use). **Not yet replayed to production.** `Levies` is still confirmed Base44-only — `autopro-syncLevies`'s port was deferred in Phase 13C specifically because that table doesn't exist yet on either branch. See `phase_13_implementation_plan.md` §0.8 for full detail.
  - **To be deleted, not migrated:** `PartsTechCart` (never lived in production), the `KanbanBoard.jsx` view, and the entire PartsTech/NAPA ProLink online-ordering feature cluster.
  - **Note (2026-08-05, not yet rolled up):** a new **Phase 15 (Final Sunset)** is planned to follow Phase 14 — it will be the actual `@base44/sdk`/`base44-proxy`/`base44/` tree removal, sequenced after a separate final-validation/blueprint-verify pass confirms every phase's features are functional and tested (so base44 context stays available for reference until the very last moment it's needed). Phase 14 itself closes with a verification step, not the sunset. Not added to the Roadmap/Time-Estimate tables yet — intentionally deferred to the rollup at Phase 14's close, per standing convention. See `phase_14_implementation_plan.md` for detail.
  - **Update (2026-08-07): one of Phase 15's preconditions is now met.** `phase_14_implementation_plan.md`'s Appendix — the standalone "Cross-Phase Base44 Residue" handoff package (23 files across the Phase 5/7/8/12/13 boundary, none owned by any active phase, explicitly outside Phase 14's own file scope) — was executed end-to-end as its own plan by a separate session, fully live-verified on `test.kensauto.ca`, and is now closed. See Section 7 for the full lessons-learned entry, including `CreditInvoice.jsx`'s full native GL-posting port (the one genuinely complex item in that batch, deployed to both dev and production) and one real bug found and fixed. Phase 14's own 14G convergence grep and the separate final-validation pass remain the only gates before Phase 15 can start.

  **Migration policy (confirmed):**
  - **Thin proxies / simple lookups & CRUD** → migrate directly to `supabase.from()` calls from the frontend (or a very thin native RPC), no intermediate function needed.
  - **Complex functions** (real business logic — GL posting, multi-step calculations, cross-table orchestration) → get a proper **1:1 native Edge Function replacement** (`autopro-*`), not a direct frontend call.
  - **Confirmed Base44-only entities** → need actual Postgres schema design (mirroring the `base44/entities/*.jsonc` definitions, following the FK/timestamp/default patterns established in `appointment_implementation_plan.md`) before any CRUD migration can happen.
  - **Confirmed dead/failed features** → delete outright, don't migrate (Kanban board, PartsTech/Online Ordering).

  **Goals:**
  1. Zero remaining `base44.*` imports anywhere in `src/`.
  2. Zero remaining `base44/functions/*` and `base44/entities/*` files still receiving live traffic — delete the tree once confirmed dark.
  3. `@base44/sdk`, `@base44/vite-plugin` removed from `package.json`; base44 plugin block removed from `vite.config.js`.
  4. `base44-proxy` Edge Function deleted once nothing calls it.
  5. Legacy `InventoryTxs` references fully removed from `src/`.
  6. No visible behavior change for end users — this is a plumbing migration, not a feature project (confirmed exceptions: Phase 13 removes the unused Kanban board, and Phase 2 removes the failed PartsTech/Online Ordering experiment).
  7. Every phase is testable against a real dev/staging environment before touching production (Phase 1 exists specifically to make this true).

  ---

  ## 2) Previously Completed (Baseline)

  **Prior work (done before this blueprint, by earlier sessions/agents, not formally tracked as a phased plan):**
  - Supabase authentication setup and Vercel hosting migration.
  - `InventoryAuditLog` established as the predominant/authoritative inventory movement tracking mechanism (superseding the legacy `InventoryTxs` entity functionally, though stale imports remain — see Phase 7).
  - 17 native `autopro-*` Edge Functions built and wired for inventory receiving/adjustment/merging/returns, WO save, GL posting (`autopro-handleInvoiceConversionGL`, `autopro-handleSupplierInvoiceLineGL`), PartsTech integration, and OCR.
  - **Customer, Vehicle, and GLTransaction are fully migrated to native Postgres** — schema, data, and backend side done. Frontend call sites still route through Base44 proxy functions for these (Phase 5).
  - **WorkPRO's entire database** (Project, ProjectTimeSession, TimeRecord, UnassignedTime, Employee, and related tables) is **fully native in the same Supabase project**, with zero live Base44 data remaining for it. AutoPRO's remaining `workProProxy` calls are legacy transport only, not a real data dependency (Phase 4).
  - **Hybrid/in-progress**: Appointment (table exists, an existing separate implementation plan — `appointment_implementation_plan.md` — governs its transition; see Phase 12), ChartOfAccount (table + data migrated, CRUD not fully cut over — low-urgency since it's a low-change-frequency table), FiscalPeriod (similarly hybrid). **`InventoryReturn` fully migrated in Phase 7** (was the one genuinely-hybrid Inventory entity; its remaining Base44-shimmed call sites are now all direct `supabase.from()` calls — no longer hybrid).
  - A local Electron Desktop App (`electron/main.js`, `electron/preload.cjs`) loading the production site — independent of the Base44 data layer, kept for its PWA-adjacent shell value.
  - `WorkPro.jsx` standalone page removed (commit `a0d3ed6`) — UI-level cleanup only; the underlying `workProProxy` calls were untouched (addressed in Phase 4).
  - The Kanban board view of Work Orders (`KanbanBoard.jsx`) is confirmed no longer in use — to be deleted, not migrated (Phase 13).
  - **(v4) PartsTech/NAPA ProLink online-ordering integration was built** (a punch-out iframe flow with LLM-based cart-text extraction, a companion Chrome browser extension for bridging cart data, and an Electron desktop cart-scraping bridge) **but is confirmed a failed experiment, not something to migrate.** Being removed outright in the new Phase 2, including its (never-applied-to-production) `PartsTechCart` staging table.
  - The old duplicate `src/components/work-orders/WorkOrderForm.jsx` (root-level, superseded by `form/WorkOrderForm.jsx`) was already deleted in commit `019a26c2` ("Online Ordering Integrations") — no cleanup action needed here.

  **Clarification on "WorkPRO" vs "AutoPRO":** AutoPRO is the shop-management system covered by this blueprint's modules. **WorkPRO is a separate sister application** — the technician-facing side: project tracking, work-order pairing with projects, and time tracking. AutoPRO reads/writes into WorkPRO's tables (already native Postgres, same Supabase project) via the legacy `workProProxy` Base44 function today. (This distinction is now also captured in `master_context.md`.)

  **What's NOT yet done (the gap this blueprint addresses):** 129 files still import the Base44 client; 279 `base44.functions.invoke` sites remain; 13 direct `base44.entities.*` CRUD sites (17 of the underlying entities confirmed to have no Postgres table at all yet); 27 `base44.auth.*` sites with no central session hook; 129 Base44-hosted functions and entity schemas still live; `@base44/sdk`/`@base44/vite-plugin` still production dependencies; no dev/staging Supabase environment currently mirrors production for safe testing (Phase 1 fixes this); the PartsTech/Online Ordering failed-experiment cluster still exists in the codebase (Phase 2 removes it).

  ---

  ## 3) Risk Assessment

  | #    | Risk                                                         | Phase(s)                                                     | Impact                                      | Likelihood                                                   | Mitigation                                                   |
  | ---- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
  | 1    | Testing against production because no dev environment exists yet | All, until Phase 1 lands                                     | **Critical**                                | High until mitigated                                         | Phase 1 is sequenced first specifically to close this gap before any data-touching phase executes. |
  | 2    | GL/double-entry accounting corruption during Banking, AP, Accounting, or Payroll rewire | 8, 9, 10, 11                                                 | **Critical**                                | Medium                                                       | Never modify the already-native GL posting functions (`autopro-handleInvoiceConversionGL`, `autopro-handleSupplierInvoiceLineGL`) — only rewire/build the CRUD feeding them. Diff Balance Sheet / GL-imbalance reports before vs. after each phase. |
  | 3    | **Confirmed** schema-design work for 17 entities with no Postgres table today — this is now a certainty, not a possibility | 8, 9, 10, 11                                                 | High                                        | **Confirmed**                                                | Design schema per entity mirroring `base44/entities/*.jsonc` definitions, following the FK/timestamp/default pattern established by `appointment_implementation_plan.md`. Build + validate in the Phase 1 dev branch before touching production. |
  | 4    | Security regression: replacing a coarse service-role proxy with a direct frontend call without proper RLS | Any phase using the "thin proxy → direct call" policy        | **Critical**                                | Medium-High if RLS skipped                                   | Every direct `supabase.from()` migration must ship with a reviewed RLS policy for that table. Complex/sensitive logic stays server-side per the stated policy. |
  | 5    | Two agents (Claude Code + Antigravity) editing overlapping files concurrently | Phases 2, 4 & 13 especially (all touch work-orders components) | Medium                                      | Medium                                                       | Serialize phases that share files; only parallelize phases confirmed file-disjoint (see Section 5 coordination map). |
  | 6    | Appointment's existing implementation plan not fully executed (title→notes migration script status unconfirmed; title column still present in DB) | 12                                                           | Low-Medium                                  | Medium                                                       | Confirm migration-script execution status before Phase 12's detailed plan proceeds. |
  | 7    | Work Orders Core (`DocumentEditor.jsx`) blast radius — touches nearly every other domain | 13                                                           | High                                        | Medium                                                       | Still sequenced after Inventory (7), Appointment (12), and PartsTech Removal (2) land. Full WO-lifecycle regression test required. |
  | 8    | Third-party integration coupling (PartsTech, LLM/OCR extraction) still partially Base44-hosted | 7                                                            | Medium                                      | Low (downgraded — PartsTech itself is being deleted in Phase 2, not migrated; residual risk here is only the unrelated OCR function) | The one still-relevant piece (`autopro-processPartsInvoiceOCR`) is unaffected by the Phase 2 deletion — confirmed it doesn't share code with `autopro-extractCartTextLLM` beyond the `GEMINI_API_KEY` secret, which must NOT be removed. |
  | 9    | Data-type corruption (string-concat bugs) re-introduced on rewritten financial write paths | 8, 9, 10, 11                                                 | High                                        | Low                                                          | Strictly cast (`Number()`/`parseFloat()`) on every rewritten write path, per existing project convention. |
  | 10   | Dev-branch secrets don't automatically carry over from production | 1                                                            | Medium                                      | High (structural — Branching doesn't clone secrets)          | Explicit secrets-setting step in `phase_1_dev_environment_parity_plan.md`. |
  | 11   | Plaintext long-lived Base44 JWT in local `.env`              | 14                                                           | Low                                         | N/A (gitignored, never committed — not a live leak)          | Your call on removal timing, not urgent.                     |
  | 12   | **(v4)** Accidentally unsetting `GEMINI_API_KEY` while removing `autopro-extractCartTextLLM`, breaking the unrelated `autopro-processPartsInvoiceOCR` function | 2                                                            | Medium                                      | Low (now explicitly flagged)                                 | Only delete the function itself; explicitly do NOT touch the shared secret. Verify OCR still works after Phase 2. |
  | 13   | **(v4)** `autopro-partstech-callback` is a webhook target — if PartsTech's system is still configured to call it, deleting it could produce a benign but confusing external error | 2                                                            | Low                                         | Low                                                          | No frontend caller found; deletion just stops accepting a webhook nobody is actively relying on. Confirm no active PartsTech account integration still points at this URL before deleting, as a sanity check. |
  | 14   | **(confirmed, structural, found during Phase 1 execution)** `VITE_BASE44_BACKEND_URL`/`VITE_BASE44_PROXY_URL` are hardcoded to production's project ref, independent of `VITE_SUPABASE_URL`. Pointing Vercel's dev environment at the dev Supabase branch does **not** isolate any `base44.functions.invoke`/`base44.entities.*`/`base44.auth.*` call from production — only direct `supabase.from()`/`supabase.functions.invoke('autopro-*')` calls are actually isolated. | All, until each phase's calls are migrated                   | **Critical for write-testing specifically** | Certain (verified)                                           | Read-only exploration of not-yet-migrated features on "dev" is harmless. **Never write-test a feature before its phase has landed** — those writes hit production regardless of environment. No config fix exists; this only resolves as each phase migrates its calls off `base44.*`. Verification steps per phase must confirm writes land in the dev branch's own tables, not just that the page loads. |

  ---

  ## 4) Time Estimate

  *Estimates in AI-paired working sessions (~2–4 focused hours each), with Claude Code and Antigravity both executing against your direction/review.*

  | ~~Phase~~ | ~~Module~~                                                   | ~~Status~~        | ~~Est. Sessions~~   | ~~Notes~~                                                    |
  | --------- | ------------------------------------------------------------ | ----------------- | ------------------- | ------------------------------------------------------------ |
  | ~~1~~     | ~~Dev & Testing Environment Parity~~                         | ~~[Tested]~~      | ~~2–3~~             | ~~See `phase_1_dev_environment_parity_plan.md`~~             |
  | ~~2~~     | ~~**(v4, new)** PartsTech / Online Ordering Removal~~        | ~~[Tested]~~      | ~~1–2~~             | ~~Deletion-heavy; low complexity but touches several unrelated surfaces (edge functions, browser extension, Electron, Setup page, one production secret)~~ |
  | ~~3~~     | ~~Auth Centralization + User→Employee Settings Migration~~   | ~~[Tested]~~      | ~~2~~               | ~~Includes one-time data migration of existing user preferences~~ |
  | ~~4~~     | ~~WorkPRO / Tech-Time Rewire~~                               | ~~[Tested]~~      | ~~2–3~~             | ~~Data fully native already~~                                |
  | ~~5~~     | ~~Customer, Vehicle & GL Cleanup (transport-layer only)~~    | ~~[Tested]~~      | ~~2~~               | ~~No schema risk; 52 call sites across 21 files migrated; 4 new native Edge Functions deployed. **Update (2026-08-07, deferred-issues follow-up, standalone bug-fix session, no formal plan document beyond `ar_deferred_issues_plan.md`):** two AR-domain gaps deferred out of this phase during a prior `processCustomerARAccounting` migration session were closed. `InterestCalculationModal.jsx`'s Calculate Interest flow got a new native `autopro-calculateARInterest` (read-only, ported 1:1 from base44 with inline date-math replacing `date-fns`'s `addDays`/`differenceInMonths`, verified byte-identical against the real library across leap-year/short-month/year-rollover edge cases before deploying). `StatementModal.jsx`'s portal-statement creation got a new native `autopro-createStatement` (JWT-decode + `Employee.mykadr_user_id` identity pattern, writes into the pre-existing `CustomerPortalStatement` table using its real `jsonb` columns, not `JSON.stringify()`). Both deployed to dev and live-verified on `test.kensauto.ca`: interest math hand-verified against raw `CustomerPayments`/`CustomerARAdjustment` data to the cent for a real customer; statement creation verified on two real customers with correct `jsonb` typing and audit attribution, throwaway test rows cleaned up with the historical 326-row count confirmed unchanged afterward. Production port explicitly not done — dev-only per standing policy.~~ |
  | ~~6~~     | ~~Reports Module~~                                           | ~~[Tested]~~      | ~~1–2~~             | ~~Isolated, read-only; 6 new native Edge Functions + 1 direct RPC swap~~ |
  | ~~7~~     | ~~Inventory Completion (incl. Category/Location/Return hybrid finish)~~ | ~~[Tested]~~ | ~~3–4~~             | ~~3 new native tables (`InventoryCategory`/`InventoryLocation`/`ReturnReason`, CSV-imported), 1 new Edge Function (`autopro-suggestInventoryCategory`, Gemini-grounded), `autopro-processQOHAdjustment` GL-rounding fix, and (out-of-plan addition) full `InventoryList.jsx` Base44 removal — all UI-verified against `test.kensauto.ca`. See `phase_7_implementation_plan.md` Section 7 for full results, deferred items (Phase 9/10/13), and untested-for-lack-of-data flows. **Update (2026-08-07, deferred-issues follow-up, standalone bug-fix session, no formal plan document):** three user-reported issues investigated and fixed. (1) OCR invoice upload's `kadr-digital_invoice_uploads` storage bucket didn't exist on the dev project at all (only on production) — created, then its two `storage.objects` RLS policies (`Allow authenticated uploads`/`Allow authenticated reads`, scoped by `bucket_id`) replicated verbatim from production via `apply_migration`, confirmed matching via direct query; the bucket-exists-but-zero-policies gap is the same landmine class as the RLS-enabled-zero-policies finding already logged for tables, just on `storage.objects` instead. (2) `TagAlong` was still entirely on the base44 entity SDK (`@/entities/TagAlong`/`@/entities/all` — an auto-generated `@base44/vite-plugin` virtual module with no real source file, confirmed via `Glob` returning nothing) across 5 files (`TagAlongManager.jsx`, `InventoryAddModal.jsx`, `InventoryEditModal.jsx`, `InventoryAdd.jsx`, `LankarImport.jsx`) — swapped to native `supabase.from('TagAlong')`, matching the table's already-open RLS and 24-char-hex id convention; `TagAlongManager.jsx`'s `OtherChargeList.list()` sibling call fixed the same way since it's required for the same feature to function. (3) `InventoryAdd.jsx`/`InventoryReturns.jsx`/`LankarImportReturnModal.jsx`'s Part # search was calling a base44 virtual function (`@/functions/searchInventory`, same no-local-file signature) instead of the already-correctly-migrated `search_inventory_ranked` RPC that `InventoryList.jsx` uses — consolidated into one new shared helper, `src/lib/inventorySearch.js`. Separately fixed a real, previously-unreported bug found while investigating: `InventoryAdd.jsx`'s New-vs-Existing-part badge (`is_existing`) was only ever set during OCR-invoice import, never during manual part-search selection or when editing-and-resubmitting a batch line — fixed by computing it fresh at the moment of Add to Batch (`handleAddToBatch`) against live `inventorySearchResults`, rather than carrying a flag through three divergent mutation paths. **Status: code-complete and `npm run build`-clean; unlike the Phase 5 items above, these Inventory items were not yet committed/pushed as of this rollup — pending deploy before live UI verification**, per the standing `test.kensauto.ca`-only-reflects-pushed-code constraint (this agent does not commit/push). Also flagged, but explicitly not fixed here (spun off as separate suggested tasks rather than expanding this session's scope): `InventoryEditModal.jsx`'s save path (`inventoryUpdate()`, `InventoryCategory.list()`, a stray `base44.functions.invoke('SupabaseProxy', ...)`) and `OtherChargesManager.jsx`'s CRUD — both still fully base44-only.~~ |
  | ~~8~~     | ~~Banking & Cash Drawer~~                            | ~~[Tested]~~ | ~~4–5~~             | ~~Complete (2026-08-03). `BankAccount`/`BankTransaction`/`BankReconciliation` — already native Postgres tables, transport-layer cutover only, like Customer/Vehicle. `CashDrawerAdjustment`/`DepositSlipBreakdown` — new native tables + production migration + full CRUD cutover. All 9 legacy functions ported to native `autopro-*` Edge Functions across 3 sub-phases (8A foundation/transport, 8B reconciliation/locking, 8C deposits/reports incl. the highest-risk `reverseDeposit`), deployed to dev + production, verified via curl, direct SQL, and live browser sessions (including a real-JWT round trip for 8C). One pre-existing UI bug found and fixed (`Reconcile.jsx` checkbox double-click no-op, 8B). Scope explicitly excludes 6 cross-domain `BankAccount`-writing files (`ReceiveCreditModal.jsx`, `SupplierPaymentModal.jsx`, `LineOfCreditPaymentModal.jsx`, `IssuedChequesTable.jsx`, and both `MarkPaidModal.jsx` files) — each carried forward to its owning phase, see notes below. **Open item carried to Phase 9 (unresolved across all of 8A/8B/8C):** `Bank.jsx`'s `handleTransfer` calls legacy `base44.functions.invoke('transferFunds', ...)` — never one of the 9 enumerated legacy functions, never assigned a sub-phase; needs a decision (fold into Phase 9, or track standalone). See `phase_8_implementation_plan.md` for full detail.~~ |
  | ~~9~~     | ~~**Accounts Payable, Suppliers, Lines of Credit & ChartOfAccount finish-up**~~ | ~~[Tested]~~ | ~~4–6~~ | ~~Complete (2026-08-03), 4 sub-phases (9A-9D). 9A: new `LinesOfCredit`/`LinesOfCreditTransaction`/`CashFlowEntry` schema (RLS-policy trap hit again, fixed) + project-wide `ChartOfAccount` transport cutover (16 files). 9B: `Supplier`/`SupplierInvoiceLine`/`SupplierPayment`/`CashFlowEntry` cutover + 8 native functions incl. `executeSupplierPayment`'s atomic `process_payment_atomic` RPC pattern; write-path UI verification blocked by the still-base44 `FiscalPeriod` gate (carried to Phase 10) but backend curl-verified. 9C: `LinesOfCredit`/`LinesOfCreditTransaction` cutover + 4 native functions (`processLineOfCreditTransaction`/`calculateLineOfCreditPaymentBreakdown`/`processLineOfCreditPayment`/`cancelLineOfCreditPayment`) + `ReceiveCreditModal.jsx`'s full 3-destination refund flow (Supplier AP/Cash Drawer/LOC) — fully live-verified including a real create→edit→delete and payment→cancel cycle. 9D: `generateChequePDF` port + `ChequeWriter.jsx` — found and fixed a genuine pre-existing bug (see Section 7) where the cheque stub's applied-invoices list never rendered for any real cheque. All 8B/8C/8D `BankAccount` cross-domain carry-forwards from Phase 8 completed alongside their owning sub-phase. **`Bank.jsx`'s `handleTransfer` → legacy `transferFunds` explicitly stayed out of scope** (Phase 9's own plan re-confirmed this at kickoff) — still unresolved, now carried to Phase 10.~~ |
  | ~~10~~    | ~~Accounting, GL Reporting, Taxes & Fiscal Periods~~         | ~~[Tested]~~      | ~~5–7~~             | ~~Complete (2026-08-03), 5 sub-phases (10A-10E). 10A: `FiscalPeriod` full transport cutover (fixed the standing 9B/9C/9D live-testing blocker) + RLS/schema fixes for `CashFlowSummary`/`GSTReturn`/`SystemSettings`/`OtherChargeList`. 10B: 9 GL/financial-report native function ports (4 already RPC-backed at the DB layer) + `postJournalEntries` port + `TechnicianPerformanceReport` payroll-target progress-bar restoration (closing the Phase 6 deferral). 10C: GST domain (`calculateGSTReturn`/`postGSTJournalEntries`/`processGSTPayment`) + `Taxes.jsx`/`MarkPaidModal.jsx` — found production's `SystemSettings` was schema-only (zero rows), seeded before deploy. 10D: `CashFlow.jsx` full cutover (fixed a `jsonb`/`JSON.parse` double-encoding bug) + `transferFunds` full native rewrite (the entity carried forward from Phase 8→9→10, finally resolved) + `Bank.jsx`'s `handleTransfer` — found and fixed a real bug, `CashFlowSummary`'s 12 `bigint` money fields silently rejecting cents. 10E: new `Levies` schema (preemptively widened to `double precision`, same bug class as 10D) + 3 native functions (`syncLevies`/`getReportableLeviesReport`/`postLeviesToAP`, the latter gaining the `checkFiscalPeriodStatus()` guard for GL-posting consistency) + 2 frontend call sites — live-verified via a real Post to AP click on `test.kensauto.ca`. All 5 sub-phases curl/SQL-verified on dev before each production deploy; `FiscalPeriod` cutover independently re-verified as unblocking the 9B/9C/9D-era blocked flows.~~ |
  | ~~10A~~   | ~~Full Inventory Flow + Appointment — Combined Testing & Cleanup~~ | ~~[Retired — folded into `blueprint_verification_plan.md`]~~ | ~~—~~ | ~~**Retired 2026-08-05.** Never executed as a standalone phase. Its entire scope — the full receive→AP→payment→GL→bank-reconciliation cross-module flow, plus closing out Phase 12's deferred Appointment live click-through items — is now tracked as a living checklist in `blueprint_verification_plan.md` (see its Section 3, the "Cross-module integration" and "Appointment" subsections, and its own explanatory note on why this row was retired). Do not resurrect this as an active phase; `blueprint_verification_plan.md` is authoritative for all remaining testing scope this row used to represent.~~ |
  | ~~11~~    | ~~Payroll~~                                                   | ~~[Tested]~~      | ~~2–3~~             | ~~Complete (2026-08-03). `PayrollTransaction` full transport-layer cutover (no schema/RLS work needed — already-native with correct RLS on both branches); `parsePayrollFile` ported byte-for-byte to `autopro-parsePayrollFile`, deployed and curl-verified on both dev and production; `MarkPaidModal.jsx`'s carried-forward `BankAccount` call site (from Phase 8) resolved, `calculateBankBalances` repointed to the already-deployed `autopro-calculateBankBalances`. 3 confirmed-dead components deleted. Live-verified end-to-end on dev: add Paycheque/Remittance/Adjustment → Mark Paid → GL postings (debits = credits to the penny) → bank balance recalculated correctly → cleanup restored baseline. One critical mid-execution correction (client-generated ids were required after all — see Section 7). File-upload parse path and the native-`window.confirm()`-gated reversal path were not click-driven live (curl-verified / code-reviewed instead, see Section 7) — everything else was. See `phase_11_implementation_plan.md` Section 5 for full detail.~~ |
  | ~~12~~    | ~~Appointment Completion~~                                   | ~~[Complete — live-testing tracked in `blueprint_verification_plan.md`]~~ | ~~2–3~~             | ~~Scoped against the existing `appointment_implementation_plan.md`. Schema fix + full frontend/reminder-function CRUD cutover done, grep/schema/eslint-verified; a real Postgres bug (`employee_id` bigint rejecting `''`) found and fixed via a live `/dev-login` session. Live click-through verification requires real cross-module data (Customer/Vehicle/WorkOrder/Employee) — the retired Phase 10A's scope for this now lives in `blueprint_verification_plan.md` Section 3's "Appointment" checklist (every item there was still unchecked as of that document's writing).~~ |
  | ~~13~~    | ~~Work Orders Core (`DocumentEditor.jsx` and friends)~~  | ~~[Tested]~~ | ~~4–6~~             | ~~Kanban board deletion + Phase 2's PartsTech removal both reduce scope here. **Update (2026-08-03):** 13A (locking + Kanban deletion) code-complete, dev-deployed, prod RPC deploy pending manual UI verification. 13B (core data backbone: `getworkorderdata`/`getworkorderlist`/`createworkorderdata`/`changeWorkOrderCustomer`/`SystemSettings`/`WorkOrderStatus`) fully code-complete, dev-deployed. 13C (business logic: `convertEstimateToWorkOrder`/`ReturnCoretoWO`/`searchWorkOrderParts` + small entity swaps) code-complete and dev-deployed except `autopro-syncLevies`, deferred — `Levies` table doesn't exist yet on either branch. 13D (Documents & Communications) and 13E (`WOAddInventoryModal.jsx` + final sweep) not started. Live-tested via real authenticated session against `test.kensauto.ca` (2026-08-03): `set_workorder_lock`, `search_work_orders`, `search_work_order_parts`, all 3 new `autopro-*` Edge Functions, and the 4 newly-native entity reads all confirmed working through real RLS/auth — see `phase_13_implementation_plan.md` §0.9 for two bugs found and fixed live (a `Promise.all` base44-coupling regression in `WorkOrders.jsx`, and a `search_inventory_ranked` overload-ambiguity bug in `GetPartModal.jsx`). `DocumentEditor.jsx` itself could not be fully UI-verified — blocked by the standing expired-`BASE44_ACCESS_TOKEN` issue (see Section 7), not by anything in this phase's own code. **Update (2026-08-03):** the `search_inventory_ranked` overload-ambiguity item flagged in this row's §0.9 reference is now resolved — stale 6-param overload dropped on both dev and production, verified via `pg_proc` + live smoke tests on both environments; see Section 7 and `search_inventory_ranked_overload_cleanup_plan.md`. **Update (2026-08-05, corrected per `blueprint_verification_plan.md`):** the 13E phase-close regression's last open item (the Counter Sale `cp_id`/`customer_complaint`/`estimated_hours`/`scheduled_date`/`technician` column fix) has since been redeployed and reverified live — Phase 13 is now `[Tested]` and fully closed. Remaining production-replay and narrower open items (WorkPRO tab not deep-tested, `GetPartModal.jsx` Enter-key behavior) are tracked in `blueprint_verification_plan.md` Section 2, not here.~~ |
  | 14        | Setup, Admin & Lankar Import (Final Sunset moved to Phase 15) | [Substantively Complete — 2026-08-07] | 2–3     | 14A-14F all code-complete and live-verified on dev; 14G's own verification pass found and fixed a real production gap (`autopro-createStatement` missing on prod, deployed same session). 2 items remain genuinely deferred, not testing-scope choices: 14F's full legacy-WO upload flow (needs a real PDF), and independent production-UI re-confirmation of the 4 dev-tested 14C Setup managers (schema/RLS already confirmed correct on production). See `phase_14_implementation_plan.md` for full detail. |
  | 15        | Final Sunset (`@base44/sdk`/`base44-proxy`/`base44/` tree removal) | [Substantively Complete — 2026-08-13] | 1–2 | Split out of the original Phase 14 scope per the 2026-08-05 rescope (§0.9). 15A-15D all complete: `@base44/sdk`/`@base44/vite-plugin` removed from `package.json`/`vite.config.js`, SDK wrapper files deleted, `base44-proxy` deleted from dev, local `base44/functions/` tree (~130 dirs) hard-deleted, 2 embedded base44 call sites rewired to native (`autopro-mergeVehicles`'s Appointment reassignment, `autopro-processQOHAdjustment`'s notification email), `npm run build` clean, full-repo `base44` grep clean except expected exceptions. Live-verified via user's own hard-test pass plus agent-side direct invocation (mergeVehicles, QOH email, Find Part search). **One item deliberately deferred, not a gap:** `base44-proxy` is still deployed on **production** — user's explicit decision (2026-08-13) to leave it until after the Aug 17 go-live cutover, since production is still running the base44-hybrid app being replaced that day and its dependency footprint there is unconfirmed; the function is already broken/unreachable for all real traffic regardless (see `phase_15_final_sunset_implementation_plan.md` 15C's traffic-corroboration finding), so leaving it deployed has zero functional cost. Also left deliberately out of scope: `base44/entities/`, `base44/connectors/`, `base44/config.jsonc` (the local repo's base44-platform-tooling remnants beyond the `functions/` tree) — never in 15C's stated scope, harmless, a future cleanup candidate. Full detail: `phase_15_final_sunset_implementation_plan.md`. |
  | **Total** |                                                              |                   | **~38–55 sessions** | Wall-clock compresses on independent phases run concurrently by both agents (see coordination map) |

  ---

  ## 5) Roadmap & Progress

  ### Parallelization & Coordination Map

| Tier #             | Phase(s)                                                     | Instructions                                                 |
| ------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| ~~**Tier A**~~     | ~~Phase 1 (Dev Env Parity)<br />Phase 2 (OL Ordering Removal)<br />Phase 3 (Auth + Employee)~~ | ~~Foundational (do first, sequentially) Phase 1 (Dev Env Parity)  -->  Phase 3 (Auth + Employee)~~ |
| ~~**Tier A - Extra**~~ | ~~File-disjoint from Phase 1 and Phase 3~~                   | ~~Independent cleanup, do early, ideally alongside/right after Phase 1 Phase 2 (PartsTech / Online Ordering Removal) File-disjoint from Phase 1 and Phase 3 — safe to run concurrently with either. Should finish BEFORE Phase 13 (Work Orders Core) and Phase 4 (WorkPRO) start, since all three touch work-orders components.~~ |
| ~~**Tier B**~~     | ~~Phase 4 (WorkPRO/Tech-Time)   <br />Phase 5 (Customer/Vehicle/GL)   <br />Phase 6 (Reports) <br />~~ | ~~Low-risk, high-leverage, mutually independent (safe to parallelize once Tier A is done) Phase 4 (WorkPRO/Tech-Time)   Phase 5 (Customer/Vehicle/GL)   Phase 6 (Reports) Phase 4 shares files with Phase 13 (below) — fine to run now, but Phase 13 must wait for Phase 4 to finish~~ |
| ~~**Tier C**~~     | ~~Phase 7 (Inventory)   <br />Phase 12 (Appointment)~~       | ~~Hybrid finish-ups, mostly independent of Tier B and each other Phase 7 (Inventory)   Phase 12 (Appointment)~~ |
| ~~**Tier D**~~     | ~~Phase 8 (Banking)~~   <br />~~Phase 9 (AP/Suppliers/LOC/)~~   <br />~~Phase 10 (Accounting/GL/Tax/ChartOfAccount)~~   <br />~~Phase 11 (Payroll)~~ | ~~Financial modules, confirmed to include real schema-design work; mostly file-disjoint from each other but share GL concepts Phase 8 (Banking)~~   ~~Phase 9 (AP/Suppliers/LOC/ChartOfAccount)~~   ~~Phase 10 (Accounting/GL/Tax) — Tested 2026-08-03, inherited and resolved `FiscalPeriod` cutover + `transferFunds` from Phase 9~~   ~~Phase 11 (Payroll) — Tested 2026-08-03, confirmed file-disjoint from Phase 10D (Cash Flow & Bank Transfer), ran side-by-side with no conflicts~~ recommend one agent owns this tier sequentially, or split by page-level file ownership if parallelizing |
| ~~**Tier D-Extra**~~  | ~~Phase 10A (Full Inventory Flow + Appointment — Combined Testing & Cleanup)~~ | **Retired 2026-08-05** — this row's entire scope (cross-module GL/inventory integration testing plus Appointment's deferred live-testing items) is now tracked in `blueprint_verification_plan.md` instead of as a blueprint phase. See that document's own note on the retirement. |
| ~~**Tier E**~~     | ~~Phase 13 (Work Orders Core)~~                              | ~~Highest blast radius, do only after 7, 12, 2, and 4 are stable Phase 13 (Work Orders Core) DO NOT run concurrently with Phase 2 or Phase 4 — all touch work-orders / WorkPRO-adjacent files~~ |
| ~~**Tier F**~~     | ~~Phase 14 (Setup/Admin/Lankar Import)~~                     | ~~Substantively complete 2026-08-07 — see Section 4 for the 2 narrow items still genuinely deferred~~ |
| ~~**Tier G**~~     | ~~Phase 15 (Final Sunset)~~                                  | ~~Substantively complete 2026-08-13 — see row 15 above for detail and the one deliberately-deferred item (production `base44-proxy`, post-go-live).~~ |

---

## 6) Verification Plan Summary

**This section is a historical snapshot of each phase's original verification scope, written as each phase closed.** For current, up-to-date testing status — including everything left unverified, every cross-phase gap, and the full itemized real-user testing checklist — see `blueprint_verification_plan.md`, which supersedes this section wherever the two conflict. Notably: Phase 12's live click-through is still outstanding (see that document's Section 3 "Appointment" checklist, entirely unchecked as of its writing), and the retired "Phase 10A" cross-module integration test never ran as its own phase — both are tracked there now, not here.

- **Phase 1 Verification:** Dev branch schema matches production; a smoke-test write in dev never touches prod; all Edge Functions + secrets present and callable on the branch; static reference tables seeded and correct[cite: 3].
- **Phase 2 Verification:** Repo-wide grep for `partstech`/`prolink`/`OnlineOrder` returns zero hits outside this phase's commit history; `WorkOrderForm.jsx`/`LineItemsTable.jsx` still function normally for all non-PartsTech actions; `autopro-processPartsInvoiceOCR` confirmed functional; `PartsTechCart` dropped[cite: 3].
- **Phase 3 Verification:** Session/role behavior identical pre/post across spot-checked pages; `/dev-login` route created and verified for issuing branch-native Supabase Auth sessions; employee preference values migrated correctly[cite: 3].
- **Phase 4 Verification:** Tech time logging and WO↔Project pairing behave identically; direct `supabase.from()` calls verified for `Project`, `ProjectTimeSession`, `TimeRecord`, `UnassignedTime`, and `Employee`; `autopro-archiveWorkOrderProjects` verified by direct invocation[cite: 3].
- **Phase 5 Verification:** Live-tested against the dev branch. Customer/Vehicle CRUD and search paths UI-tested; 4 Edge Functions (`autopro-mergeCustomers`, `autopro-mergeVehicles`, `autopro-decodeVin`, `autopro-supabaseCustomerARSummary`) verified by direct invocation[cite: 1, 3].
- **Phase 6 Verification:** Verified all 6 new Edge Functions and 1 RPC swap (`get_parts_movement_v2`); confirmed auth-gating and always-200 HTTP response shape with `{ error }` payload on failure[cite: 2, 3].
- **Phase 7 Verification:** ✅ Done (2026-08-03). Category/Location/Return CRUD confirmed hitting native tables; category AI-suggestion (Gemini-grounded) verified live; full batch-receive flow verified end-to-end through `autopro-processInventoryReceipt`; QOH-adjustment GL-rounding fix verified via direct authenticated API call (a cost/quantity combo that floats to `9.990000000000002` now posts a clean `9.99`); `InventoryList.jsx`'s search/filter/add flows (an out-of-plan addition, full Base44 removal) verified. **Not verified** (no eligible dev-branch test data at any point across the phase): `LocationModal.jsx` change/add/edit-location, parts-return/edit-return-info flows, `WarrantyReturnModal.jsx` regression, concurrent supplier-lock blocking, LANKAR/work-order-invoice warranty returns, `InventoryList.jsx`'s Delete action (non-admin test account). OCR full-flow validation explicitly deferred to Phase 9 per standing scope decision, not tested here. Full detail in `phase_7_implementation_plan.md` Section 7.
- **Phase 8 Verification:** New `CashDrawerAdjustment`/`DepositSlipBreakdown` tables validated in dev first; bank reconciliation run twice (old vs. new path) — totals match to the cent; deposit slip/cheque PDFs generate identically[cite: 3].
- **Phase 9 Verification:** ✅ Done (2026-08-03). New `LinesOfCredit`/`LinesOfCreditTransaction`/`CashFlowEntry` tables + RLS validated on dev, then genuinely-fresh-created on production (they didn't exist there) — column/FK/policy counts matched dev exactly. All 13 native functions (8 in 9B, 4 in 9C, 1 in 9D) curl-verified on dev against real/throwaway data before production deploy: `processSupplierPayment`/`executeSupplierPayment`'s atomic `process_payment_atomic` RPC path confirmed; `processLineOfCreditTransaction` verified via a full throwaway create→edit→delete cycle (GL rows + `current_balance`/`available_credit` correct at each step, reverted exactly on delete); `processLineOfCreditPayment`/`cancelLineOfCreditPayment` verified via a full throwaway LOC-to-LOC payment→cancel cycle (GL reversal rows net to zero, `is_reversed`/`reversed_by_id` cross-linked correctly both directions); `generateChequePDF` verified live end-to-end through the real UI (Cheque Register → View Cheque → rendered PDF, confirmed via blob fetch — see Section 7 for the bug found and fixed along the way). `ChartOfAccount` confirmed fully native everywhere (9A). **Not separately exercised:** `IssuedChequesTable.jsx`'s search/filter and note-editing round-trip (page load + one live cheque confirmed clean, but not stress-tested); 9B's write-path UI click-through was blocked by the still-base44 `FiscalPeriod` frontend gate (backend functions curl-verified independently) — carried to Phase 10.
- **Phase 10 Verification:** ✅ Done (2026-08-03). New `GSTReturn`/`CashFlowEntry`/`CashFlowSummary`/`Levies` tables + RLS validated on dev, then replayed to production (schema and, where found lacking — `SystemSettings`/`OtherChargeList` — data too). `FiscalPeriod` cutover (10A) confirmed live-verified, unblocking the standing 9B/9C/9D live-testing gate. GL reporting suite (10B) cross-validated against real production data: `getBalanceSheetData` returned `isBalanced: true`, `getGLAccountTransactions`'s opening balance matched a manual SQL sum exactly, `getFinancialDashboardData`'s cash position matched `BankAccount.current_balance`'s exact sum. GST calculation (10C) cross-checked against a known historical figure ($11,570.12 collected / $6,799.48 paid, matching to the cent). `transferFunds`/`CashFlow.jsx` (10D) live-verified via a real $40 UI transfer with correct GL balancing and real-user audit attribution; one real bug found and fixed (`CashFlowSummary`'s 12 `bigint` fields silently rejecting cents). `Levies` (10E) live-verified via a real Post to AP click on `test.kensauto.ca` (correct GL balance, correct real-user attribution, all affected rows flipped to `Remitted: Yes`). Zero new GL imbalances introduced across all 5 sub-phases. See `phase_10_implementation_plan.md` for full sub-phase detail.
- **Phase 10A Verification — RETIRED (2026-08-05):** This row's originally-planned scope (full end-to-end receive → AP payment → bank reconciliation → GL flow, plus closing out Phase 12's deferred Appointment click-through items) was never executed as a standalone phase. It is now tracked as a live, checkable-off checklist in `blueprint_verification_plan.md` Section 3 — see the "Cross-module integration" and "Appointment" subsections there for the current, authoritative list of what still needs a first live run.
- **Phase 11 Verification:** ✅ Done (2026-08-03). `PayrollTransaction` RLS re-confirmed correct on both branches immediately before execution (no fix needed, a rare case where the blueprint's classification held up). Full lifecycle live-tested on dev: add Paycheque/Remittance/Adjustment → select → Mark Paid → GL entries verified debits = credits to the penny with correct account mapping (5008/5009 wages, 2054 income tax, 2052 CPP, 2053 EI, 5006/5007 employer CPP/EI expense) → `BankAccount.current_balance` recalculated correctly via the repointed `autopro-calculateBankBalances` → all throwaway data cleaned up and balance restored to baseline. `autopro-parsePayrollFile` curl-verified (success + failure-shaped inputs) on both dev and production. Not click-driven live: the file-upload parse path (no file-input automation available; covered by direct curl instead) and the reversal path (`handleDelete`'s native `window.confirm()` blocked click-through in this session's browser tool; code-reviewed only). See `phase_11_implementation_plan.md` Section 5 and Section 7 below for the critical id-generation correction found mid-execution.
- **Phase 12 Verification:** Create/edit/cancel appointment round-trips match pre-migration behavior; confirm title→notes migration script ran[cite: 3].
- **Phase 13 Verification:** Full WO lifecycle regression: create → line items/parts → estimate → convert to invoice → payment → GL entries unaffected → inventory QOH correct → tech time still logs correctly; confirm Kanban board fully removed[cite: 3].
- **Phase 14 Verification:** ✅ Substantively done (2026-08-07). Scoped repo-wide grep (`base44`/`@/entities/all`/`@/functions/`) across Phase 14's own file list returns zero hits; the 4 deprecated files (`RestoreBackupModal.jsx`, `RecordDetailsModal.jsx`, `AddLegacyInvoiceModal.jsx`, `LankarImportReturnModal.jsx`) confirmed deleted and unreferenced. Full live walkthrough on `test.kensauto.ca` (dev): Setup (Sales Classes/Tagalongs/Other Charges/Work Order Status round-tripped; WIP Main/Legal/Default Message confirmed loading real data), Admin (admin-session shell load confirmed; non-admin path not separately exercised, code-reviewed only), Lankar Import (page load + `LankarWOView.jsx` regression against a real imported record, confirming a real double-unwrap bug fix), AR cluster (statement + batch work-order emails sent end-to-end with real Resend tracking IDs). **14G's own consumer-regression spot-check (checking whether anything outside Phase 14's file scope broke from the 5 newly-replayed tables) found one real, live production gap:** `StatementModal.jsx` calls `autopro-createStatement` on every open, but that function had never been deployed to production even though its target table (`CustomerPortalStatement`) was replayed there back in 14C — found, fixed (deployed to production with explicit user approval), and smoke-tested same session. Full detail, including the 2 items still genuinely deferred (not testing-scope choices), in `phase_14_implementation_plan.md`.
- **Phase 15 Verification (substantively complete 2026-08-13):** Repo-wide `base44` grep returns zero hits anywhere in `src/` (and repo-wide beyond `src/`, except planning docs and the deliberately-preserved false positives/exceptions — see `phase_15_final_sunset_implementation_plan.md`); `npm run build` clean with `@base44/sdk`/`@base44/vite-plugin` removed from `package.json`/`vite.config.js`/`package-lock.json`; `package.json`'s own `"name": "base44-app"` also renamed (`autopro-app`) as part of this pass. `base44-proxy` Edge Function deleted from **dev**; smoke-tested every top-level page (user's hard-test pass + agent live-invocation checks). `base44-proxy` on **production** deliberately left deployed — user decision, deferred to post-go-live (Aug 17) since production still runs the base44-hybrid app being replaced that day; zero functional cost either way since the function already 401s on all real traffic.

---

## 7) Lessons Learned & Context

*(Running log, carried forward into future blueprints.)*

- **2026-08-02:** Prior informal work = Supabase auth setup, Vercel hosting, and establishing `InventoryAuditLog` as authoritative. Blueprint phase numbering restarts at Phase 1[cite: 3].
- **2026-08-02:** A `base44.functions.invoke(...)` call site does not automatically mean underlying data is Base44-dependent — several proxy functions were thin forwarders to Postgres. Always confirm entity status[cite: 3].
- **2026-08-02:** WorkPRO is a distinct sister application (technician-facing), already 100% natively migrated in Supabase[cite: 3].
- **2026-08-02:** Local development is structurally impossible for this app due to same-origin auth requirements — all testing must happen via hosted deployments[cite: 3].
- **2026-08-02:** Supabase native Branching does not automatically sync schema when schema wasn't committed to git as tracked migrations. Ephemeral branches reseed from scratch; use `--persistent` for manual setup[cite: 3].
- **2026-08-02:** Commit titles and prior AI-authored plans can undersell scope or go stale (e.g. `appointment_implementation_plan.md`). Verify actual state rather than assuming a plan was executed in full[cite: 3].
- **2026-08-02:** This initiative is executed by multiple agents in parallel under user direction. Phase scoping explicitly accounts for file overlap between phases to prevent conflicts[cite: 3].
- **2026-08-02:** Dead/unused UI (e.g. `KanbanBoard.jsx`) discovered mid-planning should be deleted outright during its phase rather than migrated[cite: 3].
- **2026-08-02 (Phase 1):** Manual schema copy without checking policies left RLS enabled with zero policies, blocking access without clear errors. Always verify `pg_policies` count when copying tables[cite: 3].
- **2026-08-02 (Phase 1):** When restoring/replicating database objects (triggers especially), read full definitions first — found two production triggers with plaintext JWTs calling external webhooks[cite: 3].
- **2026-08-02 (Phase 1):** Re-verify assumptions made via CLI-only metadata against direct SQL connector queries — `PartsTechCart` turned out to exist in production even though `supabase migration list` didn't show it tracked[cite: 3].
- **2026-08-02 (Phase 1, structural finding):** `VITE_BASE44_BACKEND_URL` points to production regardless of `VITE_SUPABASE_URL`. Read-only exploration on dev is safe, but never write-test a feature before its phase has landed[cite: 3].
- **2026-08-02:** A linked planning doc written by a prior AI agent (`appointment_implementation_plan.md`) mixed actual approved scope with unsolicited feature suggestions. Don't assume everything in a prior plan is approved scope[cite: 3].
- **2026-08-02 (v4):** Always check current git history before scoping a cleanup task someone flags from memory — the root-level duplicate `WorkOrderForm.jsx` was already deleted in commit `019a26c2`[cite: 3].
- **2026-08-02 (v4):** Check whether an unresolved migration belongs to a feature about to be removed before spending effort on how to apply it, but verify live DB state directly[cite: 3].
- **2026-08-02 (v4):** When a feature cluster spans many surfaces (React modals, edge functions, browser extension, Electron protocol, dashboard buttons), perform a full codebase footprint search before scoping[cite: 3].
- **2026-08-03 (Phase 1 closeout):** Phase 1 reached "Tested" status with schema, RLS, 39 SQL functions, safe triggers, Edge Functions, and static reference tables synced. The remaining gap (`base44` calls hitting prod) is expected until individual phases land[cite: 3].
- **2026-08-03 (Phase 2 execution):** Deep file-level research before writing a plan (exact line numbers and code snippets) leads to first-pass execution success with zero surprises[cite: 3].
- **2026-08-03 (Phase 2 execution):** Source-code deletion and live Edge Function deployment deletion are two separate steps across both Supabase environments (`supabase functions delete <slug> --project-ref <ref>`)[cite: 3].
- **2026-08-03 (Phase 2 execution):** When editing a file for removal, verify whether shared imports/icons are used by neighboring code before touching the import line[cite: 3].
- **2026-08-03 (Phase 2 execution):** The "Quoted (Not Ordered)" badge in `LineItemsTable.jsx` was intentionally retained for a future feature, not tied strictly to PartsTech[cite: 3].
- **2026-08-03 (Phase 3 verification, structural finding):** Supabase branches have a fully independent Auth service from their parent project (separate JWT signing keys and `auth.users`). A production session is rejected by dev PostgREST, and a dev session is rejected by `base44-proxy`[cite: 3].
- **2026-08-03 (Phase 3 verification):** Built `/dev-login` route (`src/lib/DevLogin.jsx`) to issue branch-native Supabase Auth sessions directly for testing migrated direct-`supabase.from()` calls[cite: 3].
- **2026-08-03 (Phase 3 verification):** `npm run build` clean does NOT guarantee runtime correctness — scope errors (e.g., `currentUser={user}`) pass build steps cleanly and only throw at render time. Always click through UI[cite: 3].
- **2026-08-03 (Phase 3 verification):** Distinguish data bugs from code bugs when testing. The executive Accounting menu appeared broken because dev test data held string `'lvl3'` instead of `'lvl3_user'` enum expected by code[cite: 3].
- **2026-08-03 (Phase 3 verification):** Any Supabase call whose failure mode degrades to a plausible-looking "empty" UI state should explicitly log its `error`, not swallow it silently[cite: 3].
- **2026-08-03 (Phase 4 planning):** Audit fields (`id`, `created_date`, `updated_date`, `created_by`) do not populate themselves once proxy layers are removed. Check `information_schema.columns` and explicitly set audit fields on direct calls[cite: 3].
- **2026-08-03 (Phase 4 planning):** Before fixing or migrating a component, grep for its importers. Orphaned components should be flagged for deletion rather than blindly migrated[cite: 3].
- **2026-08-03 (Phase 4 planning, standing rule):** Leave the `base44/` source directory and live Base44 platform deployments alone until Phase 14, even once a phase fully stops calling into them[cite: 3].
- **2026-08-03 (Phase 4 planning):** A deployed `ACTIVE` Edge Function is not proof it is being called. Check actual call sites in frontend code first[cite: 3].
- **2026-08-03 (Phase 4 verification):** Don't blindly carry every field from an old proxy parameter into direct `.insert()` calls — verify target columns exist on the specific table first (`UnassignedTime` vs `TimeRecord` field differences)[cite: 3].
- **2026-08-03 (Phase 4 verification):** Dev-branch column types can diverge from production (`Employee.pay_rate` was `bigint` on dev, failing decimal inputs). Verify column data types on financial write paths[cite: 3].
- **2026-08-03 (Phase 4 verification):** The dev branch's `auth.users` password can be reset directly via SQL using pgcrypto (`update auth.users set encrypted_password = crypt(...)`) without affecting production credentials[cite: 3].
- **2026-08-03 (Phase 4 verification):** Browser automated tools cannot navigate to self-signed HTTPS dev servers (`basicSsl` plugin in Vite). Temporarily disable SSL or use standard local HTTP for automated browser testing[cite: 3].
- **2026-08-03 (Phase 5 Execution):** `npm:` vs `https://esm.sh/` specifiers matter for Deno Edge Functions importing `@base44/sdk`. Importing `@base44/sdk` via esm.sh causes worker startup failure (`WORKER_ERROR`); use Deno's native `npm:@base44/sdk@0.8.24` specifier[cite: 1, 3].
- **2026-08-03 (Phase 5 Execution):** `createClientFromRequest(req)` from `@base44/sdk` throws synchronously if Base44 SDK headers are missing. In Edge Functions invoked via `supabase.functions.invoke()`, wrap `createClientFromRequest` inside a non-fatal `try/catch` block so native Postgres operations complete regardless of Base44 auth availability[cite: 1, 3].
- **2026-08-03 (Phase 5 & 6 Execution):** **Edge Function Error-Handling Convention:** All native `autopro-*` Edge Functions must return an HTTP `200 OK` status with an `{ error: "message" }` JSON payload on failure[cite: 1, 2, 3]. The Supabase JS client swallows response bodies on non-2xx status codes (throwing `FunctionsHttpError`), hiding diagnostic details from the frontend[cite: 2, 3].
- **2026-08-03 (Phase 5 Execution):** Text columns holding stringy booleans (e.g., `Customer.is_active` and `Vehicle.is_active`) contain values like `'true'`, `'false'`, `'0'`, `'f'`, or `null`[cite: 1]. Use `.or('is_active.eq.true,is_active.is.null')` instead of plain boolean checks[cite: 1].
- **2026-08-03 (Phase 5 Execution):** `supabaseCustomerPayments` filter action special-cases `match.deposited === false` to `.or('deposited.eq.false,deposited.is.null')` and hydrates customer objects[cite: 1]. Preserved exact logic during direct query migration[cite: 1].
- **2026-08-03 (Phase 6 Execution):** Deferring unmigrated entity dependencies: `getTechnicianPerformanceReport` depended on `CashFlowSummary` (unmigrated until Phase 10)[cite: 2, 3]. Successfully migrated native utilization/efficiency logic, hardcoded progress bar values to 0, and hid the card client-side until Phase 10 restores full functionality[cite: 2, 3].
- **2026-08-03 (Phase 6 Execution):** `getWorkOrderSummaryReport`'s legacy lookup for `WorkOrderStatus` was dropped permanently after confirming `WorkOrder.status` stores plain text display names directly[cite: 2, 3].
- **2026-08-03 (Phase 6 Execution):** Drive-by field-name bug fix: `ReportModal.jsx` was checking stale `role` and `access_level` fields; updated to `admin` and `autopro_access_lvl` per Phase 3's Employee field census, restoring access to the Sales Analysis report[cite: 2, 3].
- **2026-08-03 (Phase 6 Execution):** Direct RPC migration: `PartsMovementReportModal.jsx` switched from `SupabaseProxy` to direct `supabase.rpc('get_parts_movement_v2')`[cite: 2, 3]. Note that `supabase.rpc()` returns the row array directly in `data` rather than wrapped in a proxy result object[cite: 2, 3].
- **2026-08-03 (Phase 12 Execution):** The blueprint's stale "Hybrid" classification for `Appointment` (table + data migrated, CRUD not fully cut over) was wrong on both counts — a direct query showed the native table had **zero rows** and was missing 7 of 15 fields the live UI actively wrote (`employee_id`, `status`, `reminders_*`, `reminder_days_before`). Reinforces the standing rule: always confirm entity status directly against the database, never trust a classification table at face value.
- **2026-08-03 (Phase 12 Execution):** `Postgres bigint` columns reject an empty string `''` (`22P02: invalid input syntax for type bigint`) — they need `null`, not `''`. React forms that default an FK-ish field to `''` for "unassigned"/"unselected" must convert it to `null` before hitting a bigint column. Fixed at the single shared submit choke point (`AppointmentForm.jsx`'s `handleFormSubmit`) rather than duplicating the null-coercion in every caller — worth checking for the same `''`-default pattern before wiring any other numeric-typed FK column (e.g. anything targeting `Employee.id`) to a form.
- **2026-08-03 (Phase 12 Execution):** Reconfirmed the Phase 3 Auth-isolation finding is still live and still bites: a page whose data-loading `Promise.all` mixes a still-base44-routed call with already-migrated direct `supabase.from()` calls will have the *entire* `Promise.all` reject on a dev-native session (base44-proxy 401s on a dev JWT), silently discarding the otherwise-successful native results. `Schedule.jsx`'s `loadData` hits this via the still-unmigrated `getworkorderlist()`. Not a regression to fix per-phase — it resolves naturally as each remaining base44 call gets migrated (`WorkOrder` itself in Phase 13) — but worth checking for this exact `Promise.all`-coupling pattern before assuming a page's migrated calls are broken just because the page shows empty/errors on a dev-native session.
- **2026-08-03 (Phase 12 Execution):** The `/dev-login` mechanism from Phase 3 is still fully functional and remains the correct way to live-test migrated direct-`supabase.from()` call sites — reconfirmed working against the dev branch (`test.kensauto.ca/dev-login`) nine phases later with zero rot.
- **2026-08-03 (Phase 12 closeout, user decision):** Live click-through verification that needs real cross-module data (real Customer/Vehicle/WorkOrder/Employee rows, not throwaway test data) doesn't have to block a phase's close if a later phase already plans a combined real-data testing pass. Phase 12's remaining WO-context and end-to-end appointment testing was consolidated into Phase 10A (already scoped for exactly this kind of cross-module verification) rather than left open-ended or artificially deferred to Phase 13. Consider this pattern for future phases that hit the same "code's done, but meaningful testing needs data that doesn't exist yet" wall.
- **2026-08-03 (Phase 12 → Phase 8 transition):** Before starting a new phase while another phase is actively in progress on a separate track (Phase 7/Inventory had live uncommitted edits to its own plan doc in the shared working tree at transition time), diff the incoming phase's likely touched-file set against the in-progress phase's actual documented scope — not just its module name — before assuming "different domain" means "no conflict." Found one real but narrow touchpoint this way: `src/components/inventory/ReceiveCreditModal.jsx` (Inventory directory) still writes to `BankAccount` (a Phase 8 entity) via the legacy `SupabaseProxy` shim; confirmed it's outside Phase 7's actual scope (not mentioned anywhere in `phase_7_implementation_plan.md`), so Phase 8 can proceed, but flagged for a live re-check of Phase 7's status before that one file gets touched.
- **2026-08-03 (Phase 8 planning):** Second confirmed case (after Phase 12's `Appointment`) of the blueprint's classification table being wrong at the individual-entity level within a phase — `BankAccount`/`BankTransaction`/`BankReconciliation` already existed as native Postgres tables with real data (confirmed via direct query against production, matching `src/supabase/schema.csv`'s live column dump) even though the blueprint's Phase 8 description implied full schema-design work for the whole domain. Only `CashDrawerAdjustment`/`DepositSlipBreakdown` genuinely needed new schemas. Standing rule reinforced a second time: verify entity status per-table, not per-phase-description.
- **2026-08-03 (Phase 8 planning):** When a shared entity (`BankAccount`) is written to by files scattered across many domains outside the "owning" phase's module, the user's preference is to **not** have the owning phase reach into other domains' files — instead, carry each cross-domain call site forward as an explicit note on whichever future phase actually owns that domain (Suppliers/LOC/Cheques → Phase 9, Taxes → Phase 10, Payroll → Phase 11), keeping phase boundaries aligned to domain ownership even when it means one entity's migration completes in stages across multiple phases.
- **2026-08-03 (Phase 8 planning):** Before treating an unscoped requirement (PDF generation library for a new native Edge Function) as an open research question, check whether the legacy Base44 function being ported already answers it. `base44/functions/generateDepositSlipPDF/entry.ts` and `generateDepositDetailReport/entry.ts` both already use `jsPDF` via `npm:jspdf@2.5.1` — directly portable to Deno under this project's existing `npm:` specifier convention (see the Phase 5 lesson above), no new library evaluation needed. Two files with an identical name can exist in different component folders (`src/components/bank/DepositDetailsModal.jsx` vs. `src/components/cash-drawer/DepositDetailsModal.jsx`) — the former is presentational-only, the latter holds the real `BankAccount`/`reverseDeposit`/`generateDepositDetailReport` logic; grep both explicitly rather than assuming a same-named file is a duplicate or dead code.
- **2026-08-03 (Phase 7 closeout):** The "one failed promise poisons the whole `Promise.all`" pattern (first seen in Phase 12) recurred *twice within the same phase* — `InventoryAddModal.jsx` (7A) and `InventoryAdd.jsx` (7C) both bundled a still-Base44 call (`TagAlong.list()`) with newly-migrated native calls in the same `loadData()`'s `Promise.all`, silently blocking the native results whenever the Base44 call 401'd. Standing rule reinforced a third time: whenever migrating a data-loading function, decouple every native fetch from every still-Base44 fetch into independent `try/catch` blocks — never assume porting a call "as-is" into an existing `Promise.all` is safe just because it's syntactically simple.
- **2026-08-03 (Phase 7 closeout):** `id` column conventions are not uniform even within one migration phase. Phase 7's three new tables (`InventoryCategory`/`InventoryLocation`/`ReturnReason`) use 24-char hex (Base44 ObjectId-style) IDs matching their CSV exports, but the pre-existing `InventoryItem` table uses standard 36-char `crypto.randomUUID()` IDs — confirmed by direct production query before writing a new `.insert()` for it. Never assume one ID format is project-wide; check a real row on the specific table first.
- **2026-08-03 (Phase 7 closeout):** A `.sql` file checked into the repo (`src/supabase/search_inventory_ranked.sql`) does not necessarily match what's actually deployed — its `p_include_inactive` parameter doesn't exist on the live function (dev branch confirmed via `pg_get_functiondef()`; only 6-param and 8-param overloads exist). Always verify an RPC's real signature against the live database before depending on a parameter the repo's source claims to support.
- **2026-08-03 (Phase 7 closeout):** The expired `BASE44_ACCESS_TOKEN` (Phase 5/6/12's recurring 401 source) is a standing, project-wide infra issue, not fixed by any single phase's migration work — it will keep producing "broken" symptoms (employees, work orders, settings, `TagAlong`, and any other still-Base44 call) on every page until either that page migrates off Base44 or someone refreshes the token from a fresh admin Base44 session cookie. Worth a dedicated decision (refresh vs. accelerate migration) rather than re-diagnosing it fresh each phase.
- **2026-08-03 (Phase 7 closeout):** Scope can legitimately grow mid-close-out when UI testing surfaces a blocking, closely-related issue — `InventoryList.jsx`'s full Base44 removal wasn't in Phase 7's original file list (only its `loadSharedData` category/location reads were), but its search/list/add/delete calls were blocking the same "Add Item" flow this phase's testing depended on, traced to the same token-expiry issue above. Migrated in the same session rather than deferring, since the fix was the same well-established pattern (direct `supabase.from()`/`.rpc()` calls) already used everywhere else this phase.
- **2026-08-03 (Phase 13B/13C execution):** A phase-planning doc's own assumption about entity status can be wrong even when written carefully — 13B's plan assumed `SystemSettings`/`WorkOrderStatus` were straightforward native-table swaps (like Phase 5-7's precedent), but a direct `information_schema.tables` check (across all schemas, both dev and prod) showed neither existed anywhere. The same check caught `TagAlong`/`OtherChargeList`/`Levies` missing too. Corrected by deferring those specific scope items rather than guessing a schema and creating tables unprompted — mid-execution the user then created `SystemSettings`/`WorkOrderStatus`/`TagAlong`/`OtherChargeList` on the dev branch directly (RLS enabled with zero policies on all four — the Phase 1 landmine recurring a second time, fixed the same way), unblocking those conversions same-session. `Levies` remains uncreated; `autopro-syncLevies` stays deferred. Reinforces: `@/entities/all`'s import path is never proof an entity is native — only a direct schema query is.
- **2026-08-03 (Phase 13B execution):** `WorkOrder.id` follows the same "check live data before assuming an ID format" lesson from Phase 7 (§ above) — it's a 24-char truncated-hex string (`crypto.randomUUID().replace(/-/g,'').substring(0,24)`), not a standard 36-char UUID, confirmed by querying real production rows before writing the native `.insert()` for `createworkorderdata`'s replacement. A bare `crypto.randomUUID()` would have silently produced inconsistently-formatted ids alongside 1,557+ existing rows.
- **2026-08-03 (Phase 13 execution):** The `search_inventory_ranked` overload-ambiguity issue flagged at Phase 7 closeout (only 6-param and 8-param versions exist live, no `p_include_inactive`) recurred in a brand-new call site — `GetPartModal.jsx`'s two new RPC calls passed only `p_search_term`/`p_limit`, which PostgREST can't resolve between the two overloads (`300 "Could not choose the best candidate function"`, caught live against `test.kensauto.ca`). Fixed by passing `p_location_from: null, p_location_to: null` explicitly to disambiguate. The underlying duplicate-overload cleanup itself is still not done — flagged again for whoever next touches this RPC, since any future caller that copies a "just pass search term and limit" example will hit the same wall.
- **2026-08-03 (Phase 13 execution):** The "one failed promise poisons the whole `Promise.all`" pattern (Phase 12, Phase 7 ×2) recurred a fourth time — `WorkOrders.jsx`'s `loadData()` bundled the still-base44-routed `getNotesBoardData` call with the two newly-native `search_work_orders` RPC calls in one `Promise.all`; the base44 call's 401 (see the `BASE44_ACCESS_TOKEN` entry above — still unresolved) blanked the entire Work In Progress tab even though both RPC calls succeeded. Fixed by giving `getNotesBoardData` its own `.catch()`. The standing rule from Phase 7 closeout (decouple every native fetch from every still-Base44 fetch into independent error handling) should probably be applied proactively to every remaining mixed `Promise.all` still in the codebase, rather than caught one page at a time as each gets tested.
- **2026-08-03 (Phase 13 execution):** The expired `BASE44_ACCESS_TOKEN` issue (first flagged at Phase 7 closeout) is still unresolved and still blocking UI verification — this time on `DocumentEditor.jsx`, via `useShopData()`'s (`useInventory.jsx`, Phase 7 domain) base44-routed `InventoryItem`/`Employee.list()` calls. Confirmed the blocker is that hook, not Phase 13's own code, by independently validating every native call `DocumentEditor.jsx` actually depends on (`WorkOrder`/`SystemSettings`/`WorkOrderStatus` reads, `set_workorder_lock`) via direct authenticated `fetch()` against the real session — all returned 200 with correct data. **Live-testing technique worth reusing directly:** decode the `supabase-auth-token` cookie already set by `/dev-login`, then call REST/RPC/Edge-Function endpoints directly with that JWT plus the project's anon key — this validates real RLS/auth behavior and works even when the frontend can't fully render due to an unrelated blocker like this one.
- **2026-08-03 (Phase 7 closeout, testing-tool finding):** The Browser-pane automated testing tool's synthetic "Enter" keypress (`computer` tool's `key` action) does not set `event.key` to `"Enter"` — it arrives as an empty string. Any component logic gated on `e.key === 'Enter'` will silently fail to trigger under this specific automation path even though it works for real users. Workaround: dispatch a real `KeyboardEvent({key: 'Enter', bubbles: true})` via `javascript_tool` instead of relying on the `computer` tool's key action, whenever a component depends on the exact `event.key` value.
- **2026-08-03 (Phase 8C closeout, testing-tool finding):** The Browser-pane `computer` tool's synthetic click (via `ref`) did not reliably trigger React `onClick` handlers in this session — clicking a `[role="dialog"]`-opening button by `ref` repeatedly produced no DOM change, while calling `.click()` on the same element directly via `javascript_tool` worked every time. Workaround: prefer `javascript_tool` with a targeted `querySelector`/`Array.from(...).find(...)` + native `.click()` over the `computer` tool's ref-based click when a click doesn't appear to register — cheaper to diagnose than assuming the app itself is broken.
- **2026-08-03 (Phase 8C closeout, novel technique):** For any new native `autopro-*` Edge Function that needs to write real audit-trail fields (`created_by`/`created_by_id`) without a base44 session available (the established convention drops the base44 auth gate entirely, per the Phase 8B cross-cutting finding) — extract identity from the caller's own Supabase JWT via `supabase.auth.getUser(token)`, reading `req.headers.get('Authorization')`. This is safe specifically when the function deploys with `verify_jwt: true`: the platform has already validated the token before the function runs, so this call only extracts identity, it isn't functioning as an access gate, and it correctly resolves to the real signed-in user's email/id for a genuine session while degrading gracefully (`'Unknown User'`/`null`) for a non-user caller like a raw anon-key `curl` test. First needed in `autopro-reverseDeposit` (8C) — worth reusing directly for any future port that writes audit fields without an available base44 session, rather than re-deriving the pattern.
- **2026-08-03 (Phase 8C closeout):** The "one failed promise poisons the whole `Promise.all`" pattern (Phase 12, Phase 7 ×2, Phase 13) recurred a fifth time — `DepositHistoryModal.jsx`'s `loadDeposits()` bundles the already-native `autopro-getBankTransactions` call with a still-base44-routed `FiscalPeriod.list()` in one `Promise.all`; `FiscalPeriod`'s 401 (it's an explicitly out-of-scope "hybrid" entity per Section 4's classification) blanks the whole deposit-history list even under a real authenticated session, blocking the only in-app path into `DepositDetailsModal`'s reverse/report buttons. Pre-existing, not introduced by Phase 8 (this exact `Promise.all` wasn't touched by 8A/8B/8C — `getBankTransactions` was already native since 8A). Not fixed here since `FiscalPeriod` migration is cross-cutting to multiple future phases, not Phase 8's to own — but whichever phase finally migrates `FiscalPeriod` off base44 should specifically check this file, since it's an easy one to miss (the symptom looks like "deposit history is just empty," not an obvious error).
- **2026-08-03 (Phase 13 correction, rolled up from `phase_13_implementation_plan.md` §0.6):** `WorkOrder.line_items` core-tracking fields are `Core_num` (capital C), `core_ret`, `core_cost`, and a *computed* `core_osamt` (`= (Core_num - core_ret) * core_cost`) — confirmed directly against code and live data. An earlier Phase 7 planning note claiming a `core_onhand` field was wrong; **no such field exists anywhere**. No Phase 7 code was ever written against the wrong name (planning-doc-only error), but any future phase touching core tracking should use the four fields above, not `core_onhand`.
- **2026-08-03 (Phase 13D, rolled up from `phase_13_implementation_plan.md` §0.5):** `jsPDF` (`npm:jspdf@2.5.2`) confirmed working end-to-end for hand-rolled vector/text PDF generation inside a Deno Edge Function runtime, via `autopro-generateWorkOrderPdf`'s live-tested port of the legacy `generateWorkOrderPdf` function (all 4 stages verified against real data). This directly de-risks the identical approach already flagged as viable for Phase 8's `generateDepositSlipPDF`/`generateDepositDetailReport` (see the Phase 8 planning entry above) — now confirmed, not just plausible.
- **2026-08-03 (Phase 13E + phase-close regression, `test.kensauto.ca`):** A phase's own migrated file being clean is not proof the surrounding lifecycle is bug-free — running the *actual* full WO lifecycle live (create → estimate → parts/core/other-charge → convert to WO → receive → convert to invoice → payment → PDF/portal/SMS/email → void/core-return/warranty-return → notes/locks) for the first time since the whole chain went native surfaced **6 real, previously-unexercised bugs across 8 files**, none in the sub-phase's own nominal scope: (1) `WorkOrders.jsx`'s Counter Sale insert referenced two separate sets of non-existent `WorkOrder` columns (`cp_id`; then `customer_complaint`/`estimated_hours`/`scheduled_date`/`technician`) — the working "New Work Order" modal path never hit either because its own payload construction happens to omit them; (2) five files (`FinancialSummary.jsx`, `WorkOrderViewFinancialSummary.jsx`, `AdvancePaymentModal.jsx`, `SESEmailModal.jsx`, `BatchSendWorkOrdersModal.jsx`) never received the `typeof x === 'string' ? JSON.parse(x) : x` jsonb-guard already correctly applied to `WorkOrder.payments` in `DocumentEditor.jsx`/`InvoiceConversion.jsx`; (3) `WOAddInventoryModal.jsx`'s existing-part batch-add path did unguarded `stringColumn + number` arithmetic on `InventoryItem.quantity_on_order`/`quantity_on_hand` (both `text`-typed columns) — silent JS string-concatenation, not addition (`"1" + 1 = "11"`) — a genuine **inventory-data-corruption bug**, the worst-severity finding of this pass. Standing takeaway: schedule a real, live, full-lifecycle click-through as its own late-phase gate even when every individual file's own targeted tests already pass — it catches an entirely different class of bug (cross-file assumptions, cold-path branches, cross-cutting `text`-column arithmetic) that per-file testing structurally cannot.
- **2026-08-03 (testing-tool finding, extends the Phase 7/8C entries above):** Radix UI's `Tabs` and `AlertDialog`/`Dialog`-trigger primitives respond differently to synthetic interaction. Plain buttons with a direct `onClick` (dialog triggers, form submits) respond reliably to `javascript_tool`'s native `.click()`. Radix `Tabs` (`[role="tab"]`) do **not** — `.click()`, and even manually dispatched `pointerdown`/`mousedown`/`pointerup`/`mouseup`/`click` sequences, leave `data-state` unchanged; only the `computer` tool's real synthetic click (via a freshly-read `ref`, not a stale one) reliably activates them. Separately, a Radix `Dialog` can be fully mounted and interactive (`data-state="open"`, correct content, real form fields) while `getBoundingClientRect`/`offsetParent`-based visibility heuristics falsely report it as hidden — this is a `position: fixed` quirk (`offsetParent` is `null` for fixed-position elements in most browsers), not a real rendering bug. When a click seems to do nothing: try the other technique before concluding the app itself is broken. Also: `window.confirm()` always returns `false` under this environment's dialog suppression, silently no-op'ing any flow gated on it — override with `window.confirm = () => true` for the duration of a test session when a flow is known to use a real (non-suppressible) confirmation gate.
- **2026-08-03 (Phase 8 closeout, live-verification technique):** When a live UI action's success/failure is ambiguous from DOM/dialog state alone (e.g. a Radix dialog mid-teardown-animation showing a half-cleared render that looks identical whether the underlying async call succeeded or hit its catch path), don't guess — extract the real session's JWT from the `supabase-auth-token` cookie (same technique as the Phase 13 entry above) and replicate the exact frontend call directly via `fetch`/`javascript_tool`. This proves definitively whether the deployed function works with the exact auth a real browser sends, independent of any UI automation quirk, and was the deciding evidence for both `autopro-generateDepositSlipPDF` and `autopro-reverseDeposit` in 8C's close-out.
- **2026-08-03 (Phase 13 §0.9 follow-up, RESOLVED):** The `search_inventory_ranked` duplicate-overload issue flagged twice above (Phase 7 closeout's `p_include_inactive` discrepancy, Phase 13 execution's `GetPartModal.jsx` `PGRST203` ambiguity) is now fully closed — the stale 6-param overload was dropped via a tracked migration (`drop_stale_search_inventory_ranked_6param_overload`) on both dev (`sitihbdnuxifwibontcm`) and production (`hbcrwkmgsazqrvsrmxyr`), confirmed via direct `pg_proc` introspection that exactly one (8-param) overload remains on each, and smoke-tested against live data on both environments with both real calling-convention shapes (`null`/`null` and `''`/`''`) before and after the drop. The checked-in `src/supabase/search_inventory_ranked.sql` was also rewritten to match the live `pg_get_functiondef()` output verbatim, finally removing the phantom `p_include_inactive` parameter that never actually existed on any deployed version. **Reusable lesson:** a `DROP FUNCTION` targeting the full explicit parameter-list signature (not just the function name) is what makes dropping one specific overload safe — Postgres will refuse to match a different overload's signature, eliminating the "dropped the wrong one" risk entirely. Full detail in `search_inventory_ranked_overload_cleanup_plan.md`.
- **2026-08-03 (Phase 9 execution):** A native `jsonb` array column comes back from `supabase-js` already deserialized into a real JS array — calling `JSON.parse()` on it (a pattern several legacy Base44 functions use, written when the column held a JSON-encoded *string*) throws, gets silently caught, and produces an empty result with zero visible error. Found in `generateChequePDF`'s `SupplierPayment.invoice_number` handling (confirmed via direct production query: real rows are true jsonb arrays, not strings) — the cheque stub's "applied invoices" list had never rendered for any real cheque, in the legacy function or a faithful port of it. Fixed with a `safeParseJsonArray` guard (`Array.isArray(value) ? value : JSON.parse(value)`) — the same defensive pattern a sibling legacy function (`cancelLineOfCreditPayment`) already used, just missing here. **Reusable check:** before porting any `JSON.parse(payment.someField)`-shaped legacy code, confirm the target column's actual Postgres type first — a `jsonb`/`json` column needs the safe-parse guard, a `text` column doesn't.
- **2026-08-03 (Phase 9 execution):** A cross-account reversal can be asymmetric by design in the legacy code, not by omission — `cancelLineOfCreditPayment`'s `other_line_of_credit` branch fully reverses the *target* LOC's transaction/GL/cross-links but never restores the *source* LOC's `current_balance`/`available_credit`. Confirmed via live curl testing (had to manually SQL-restore the source LOC's balance after a throwaway cross-LOC payment/cancel test). Ported byte-for-byte per standing convention rather than silently fixed, but flagging here since it's a real, user-visible gap a future phase or bug report might rediscover from scratch.
- **2026-08-03 (Phase 9 execution, testing technique):** `test.kensauto.ca` and the `sitihbdnuxifwibontcm` dev branch are the same environment — confirmed by checking whether the throwaway `Test Supplier` (id `999999999`, seeded during Phase 9's own dev-branch testing) is visible on the live site. Reusable sanity check before inserting any throwaway live-test data: if a known dev-only seeded row shows up, it's safe to test against; don't assume from the domain name alone.
- **2026-08-03 (Phase 9 execution, process finding):** Don't assume a live site is still running old frontend code just because you haven't personally pushed this session's changes — `git log` showed a `Phase 9D`/two `Commit` entries already on `origin/development` mid-session (the user commits via GitHub Desktop independently, per standing project convention), meaning the new native-Supabase frontend code was already live before this session explicitly checked. When live-verification results seem surprising (e.g., a user reports success testing a feature you haven't seen them push), check `git log`/`git status` for `up to date with origin` before assuming stale code is the explanation.
- **2026-08-03 (Suppliers, user-reported glitch — RESOLVED):** `Supplier.LockedByUser` edit locks were getting stuck permanently for any Cheque payment. Root cause: `SupplierPaymentModal.jsx`'s cheque-success handler does a hard `window.location.href` redirect to `ChequeWriter` (not a React Router navigation), which bypasses every one of `SupplierTx.jsx`'s working lock-release paths (Back button, the intercepted nav-menu click in `Layout.jsx`). The only remaining fallback was the `beforeunload` handler's `releaseLock` — itself a two-step awaited `SELECT`-then-`UPDATE` — and browsers don't reliably let async work finish once a real navigation/unload starts, so that call routinely lost the race. Confirmed live end-to-end: found a real supplier (`DENHAM CHRYSLER JEEP LTD.`) stuck locked on production since March; reproduced the exact bug on the dev branch by running a real cheque payment through the actual UI before the fix existed (lock stayed set), then confirmed the same flow clears the lock after the fix. Two alternate theories were checked and ruled out: an email-casing mismatch between the Auth-JWT email (used by `acquireSupplierLock`) and the Employee-record email (used everywhere else for comparison) — confirmed byte-identical for the stuck row; and the payment backend itself (`autopro-processSupplierPayment`, `autopro-executeSupplierPayment`, `process_payment_atomic` RPC) touching `Supplier.LockedByUser` — confirmed none of them do. This was purely a frontend navigation-bypass bug.
  **Fix:** no base44 function ever handled supplier-lock *release* (`base44/functions/acquireSupplierLock/entry.ts` only ever did acquire) — built a new native `autopro-releaseSupplierLock` Edge Function from scratch (single atomic `UPDATE ... WHERE id = ? AND LockedByUser = ?`, no check-then-update race window), deployed to both dev and production. Reused the `fetch(..., { keepalive: true })` pattern `DocumentEditor.jsx` already established for WorkOrder locks (`postKeepAliveFunction`) — factored into a small shared helper, `src/components/utils/supplierLockUtils.jsx`'s `releaseSupplierLockKeepAlive` — but pointed at the new native function rather than `DocumentEditor.jsx`'s own base44-proxied `manageWorkOrderLock` endpoint, since Suppliers have no base44 dependency to preserve. Wired into `SupplierPaymentModal.jsx`'s cheque redirect and `SupplierTx.jsx`'s `beforeunload` handler; `SupplierTx.jsx`'s normal `releaseLock` (unmount/Back button/nav-menu — already reliable) was simplified to call the new single-round-trip function too, removing its own check-then-update race window as a side benefit.
  **Reusable lesson:** a `keepalive: true` fetch is the correct fix whenever code must reliably notify the backend right before a *hard* page navigation (`window.location.href`, not React Router's `navigate()`) or during `beforeunload`/`pagehide` — a normal awaited `fetch`/`supabase-js` call has no such guarantee and will frequently lose the race. Worth checking for the same pattern (a raw `window.location.href` assignment mid-flow that skips a page's own cleanup/lock-release logic) anywhere else in the app.
- **2026-08-03 (Phase 9 execution, tooling finding):** Two distinct browser-automation surfaces exist and behave very differently: the isolated Browser pane (`Claude_Browser__*`) is a separate browser instance with no shared cookies/session — logging into it requires typing credentials, which is a hard restriction the agent itself cannot do (even test/dev credentials) per standing safety rules; "Claude in Chrome" (`claude-in-chrome__*`) drives the user's actual Chrome and inherits whatever session is already open there, but requires the Chrome extension to be installed and signed in, and can silently be "not connected" even when the user believes it's set up. When one surface is blocked, the other is often the faster unblock — but confirm which one actually has the needed authenticated session before spending time troubleshooting the wrong one.
- **2026-08-03 (Phase 9 closeout, carried forward):** `Bank.jsx`'s `handleTransfer` → legacy `base44.functions.invoke('transferFunds', ...)` remains unresolved — flagged at Phase 8's 8A/8B/8C close, explicitly re-declined at Phase 9's own kickoff (its plan's Section 1 lists it "still an open Phase 8 gap, not Phase 9's to resolve"), now carried to Phase 10 since no phase has claimed it twice in a row. Whoever scopes Phase 10 should either take it or make an explicit, documented decision to defer it again rather than let it silently drop a third time.
- **2026-08-03 (Phase 11 execution, critical correction to the phase's own plan):** A phase-planning doc's stated assumption about id generation can be wrong even after an explicit check — Phase 11's plan (§2) stated "none of this phase's creates currently generate an id client-side... confirm this holds before assuming `.insert()` without an explicit `id` field is safe," and then never actually confirmed it before writing the first native insert. Direct SQL against dev showed `PayrollTransaction`, `BankTransaction`, and `BankAccount` have **no working `id` default at all**, and `GLTransaction.id`'s default is a decoy (`''::text` — present, but useless, since it produces a non-unique empty string rather than a real id). Every id on every existing row in these tables was actually generated by the legacy `SupabaseProxy` shim's `buildCreateRow()`, which silently does `crypto.randomUUID().replace(/-/g, '').substring(0, 24)` for every table except `Note` — the same 24-char-hex convention already used natively in ~15 other already-migrated files (`Bank.jsx`, `CashFlow.jsx`, `WorkOrders.jsx`, etc., per the Phase 7/13 entries above). Caught before it could fail live testing, but only because it was re-verified against real `pg_attrdef` output rather than trusted from the plan doc. **Standing rule, now confirmed a second time (first at Phase 7 closeout for a different table): before writing the first native `.insert()` on any table, directly query whether its `id` column has a real, working default — a present-but-empty default (`''::text`) is not the same as "no default," and both are traps a plan's own stated assumption can get wrong.**
- **2026-08-03 (Phase 11 execution, deploy-pipeline finding):** `test.kensauto.ca` serves whatever is currently pushed to the `development` branch via an existing Vercel pipeline (`vercel.json` confirms auto-deploy) — local uncommitted code changes are invisible there regardless of how confident the local build looks. Concretely hit this: the first live-verification attempt against `test.kensauto.ca` 401'd on a call to the legacy `SupabaseProxy` function, which made no sense until realizing the site was still running the pre-migration build because nothing had been pushed yet. Once the user pushed via GitHub Desktop (their standing workflow — commits/pushes are never done by the agent), the same page loaded cleanly against the new native code. **Reusable sequencing point:** curl-verification of a new Edge Function doesn't depend on this (it's deployed independently via the Supabase MCP tools), but any live *browser* verification against `test.kensauto.ca` does — confirm the push landed (or ask the user to push) before diagnosing a live-site failure as a code bug.
- **2026-08-03 (Phase 11 execution, testing-tool finding):** The already-documented `window.confirm()` override workaround (see the Phase 7/8/9 testing-tool entry above — `window.confirm = () => true` via `javascript_tool`, needed because this environment's dialog suppression makes native `confirm()` calls resolve to `false`) applies to the isolated Browser pane (`Claude_Browser__*`) just as much as whatever surface it was first found on — it wasn't applied during Phase 11's `Payroll.jsx` `handleDelete` reversal-path test, so that click-through was left as code-review-only rather than live-verified. Re-flagging so it's actually applied next time: check the lessons log for a matching testing-tool workaround *before* concluding a UI path structurally can't be automated, rather than after.
- **2026-08-03 (Phase 10A/10C/10D/10E, recurring finding):** "Replayed to production" can silently mean *schema only*, not data — happened three separate times in this one phase. 10A's own task list claimed `SystemSettings`/`CashFlowSummary`/`OtherChargeList` were replayed to production, but 10C.0's pre-flight found all three had zero rows there (only the empty `CREATE TABLE` had been run); 10D.0 re-confirmed the same gap for `CashFlowSummary`/`OtherChargeList` independently; 10E.0 found `OtherChargeList` still empty on production going into its own execution. **Standing rule now confirmed three times over:** after any "replay schema to production" step, explicitly verify row counts match dev too (`SELECT count(*)` on both branches) — a table existing is not proof it's usable, and a function that reads a required settings/reference row will fail with a generic error (`'System settings not found'`, `'No levies found'`) that looks like a code bug, not a data gap.
- **2026-08-03 (Phase 10D, real bug found and fixed):** A `bigint` column silently rejects any value with cents (Postgres `22P02`), and if the calling code's `{ error }` check on the PostgREST response is missing or incomplete, the failure is **completely invisible to the user** — the UI shows the new value optimistically, nothing persists, and a reload silently reverts it with zero error message. Found in `CashFlowSummary` (12 money-estimate fields were `bigint` instead of `double precision`) via a scripted `window.fetch` patch that confirmed the PATCH request was firing correctly, then reproducing the exact request via curl to surface the real Postgres error. **Reusable check:** any `bigint`-typed column intended to hold a dollar amount is a latent version of this same bug — widen to `double precision` proactively rather than waiting for a user to hit it. 10E applied this proactively to `Levies.total_amount`/`base_amount` before any real fractional value had ever been written to them, based on this exact lesson.
- **2026-08-03 (Phase 10D/10E, GL-posting consistency):** Not every legacy GL-posting function checked `FiscalPeriod` status before writing — `transferFunds` and `postLeviesToAP` both omitted the guard that 5 sibling functions (`processLineOfCreditPayment`, `cancelLineOfCreditPayment`, `processLineOfCreditTransaction`, `reverseDeposit`, `processInventoryReceipt`) already had. This is inconsistent within the legacy codebase itself, not a hidden requirement — both omissions were confirmed via full legacy source reads, then closed by adding the same 15-line `checkFiscalPeriodStatus()` guard (copy-pasted forward from `processLineOfCreditPayment`'s pattern) to both native ports, verified live to correctly reject a closed-period date with zero writes. Every real-money-movement `autopro-*` function in the project now has this guard — worth checking for on any newly-discovered GL-posting function that predates this convention.
- **2026-08-03 (Phase 10E, response-shape migration trap):** `base44.functions.invoke(name, body)` returns the function's JSON body nested one level deeper (`response.data.data`, `response.data.success`) than native `supabase.functions.invoke(name, { body })`'s `{ data, error }` shape, where `data` **is already** the function's JSON body directly. A mechanical find-replace of the call itself without also dropping one `.data` level produces working-looking code that silently always hits the "else" branch (`response.success` undefined → falsy) — no runtime error, no console warning, just a report page that never shows data or an AP-post that "always fails." Caught here specifically because `ReportableLeviesReport.jsx` had two call sites doing this unwrap; both fixed and live-verified. Worth grepping for `.data.data` or `.data.success` patterns in any file still pending this exact migration.
- **2026-08-03 (Phase 10E, testing-tool finding):** A work-order row in the WIP list can enter a "Checking..." lock-status state on click and silently bounce back to the list without opening, under the automated Browser pane — distinct from the already-documented Radix-Tabs/Dialog and `window.confirm()` quirks (see the Phase 7/8/9 testing-tool entries above). Not diagnosed further this session (declared out of scope for a Levies-domain phase to chase); the underlying `autopro-syncLevies` logic it would have exercised was already independently curl/SQL-verified, so this was accepted as a live-testing gap rather than blocking phase close. Worth a fresh diagnosis attempt (real click event sequence via `javascript_tool` rather than the `computer` tool's ref-click, per the Phase 8C-established technique) the next time a phase actually needs to click into a WIP work order live.
- **2026-08-03 (Phase 10E, live financial-action verification technique):** For a real (non-throwaway) financial action gated behind `window.confirm()` on a live environment — e.g. "Post to AP" against real unremitted data, not synthetic test rows — get explicit user sign-off before clicking, since it's a real accounting mutation even on the dev branch, not reversible without manual cleanup. Once approved, the standing `window.confirm = () => true` override (Phase 7/8/9/11 entries above) plus a post-click direct-SQL check of the resulting rows' audit fields (`created_by`/`created_by_id` reflecting the real logged-in user, not the `System` curl-fallback) is the fastest way to get genuine proof the live UI path works end-to-end, not just that the backend function works in isolation.
- **2026-08-07 (Appendix Cross-Phase Residue Cleanup, standalone effort, `GLTransaction` decoy-default id trap — at least 4th confirmed recurrence):** `InventoryEditModal.jsx`'s cost-change GL-posting block was migrated "transport-only" (per this migration's own standing policy — preserve the debit/credit business logic, only swap the `base44.functions.invoke('SupabaseProxy', ...)` call for `supabase.from('GLTransaction').insert(glTransactions)`), but the `glTransactions` array entries never had client-generated `id`s — the base44 `SupabaseProxy` shim had been silently generating them, same failure shape as the Phase 11 entry above. `GLTransaction.id`'s `''::text` decoy default meant the 2-row batch insert's first row claimed the empty string and the second failed with `23505 duplicate key`, leaving the `InventoryItem.cost` write applied without its compensating GL entries (a genuine partial-failure state, only caught by a live UI write — `npm run build`/`eslint` are structurally incapable of catching this class of bug). Fixed with the standard `crypto.randomUUID().replace(/-/g,'').substring(0,24)` id on each GL-entry constructor; re-verified live post-fix. **Standing rule now confirmed a fourth-plus time:** a "transport-only" swap is not automatically safe just because the business logic is untouched — any insert into a table with a present-but-non-working default (`GLTransaction.id`'s `''::text` is the single most recurring offender in this entire migration) needs its own explicit id-generation check, every single time, regardless of how mechanical the rest of the swap looks.
- **2026-08-07 (Appendix Cross-Phase Residue Cleanup, standalone effort, closeout):** The Phase 14 Appendix's 23-file "Cross-Phase Base44 Residue" handoff package (deliberately scoped outside Phase 14's own file ownership, left over from the Phase 5/7/8/12/13 boundaries, converging with Phase 14 only at 14G's grep check) was executed end-to-end as its own standalone plan by a separate session running in parallel with Phase 14 proper — full detail in `phase_14_implementation_plan.md`'s "Appendix Closeout" section. The one genuinely complex item, `CreditInvoice.jsx`, got a full native GL-posting Edge Function port (`autopro-handleCreditInvoiceGL`), deployed to both dev and production, live-verified end-to-end on dev with a real credit invoice (GL debits/credits balanced to the penny, `jsonb` columns confirmed genuine arrays not double-encoded strings) and confirm-only smoke-tested on production. **Notable technique reused successfully from Phase 8C/13:** when a browser-automation click doesn't register (a Radix `DropdownMenu`/`Select` trigger silently no-opping under a plain `.click()` or `computer`-tool ref click), dispatching a manual `pointerdown`/`mousedown`/`pointerup`/`mouseup`/`click` `PointerEvent`/`MouseEvent` sequence via `javascript_tool` at the element's real `getBoundingClientRect()` coordinates reliably unstuck it — worth trying before concluding a UI path is broken, consistent with the existing testing-tool lessons logged above.
- **2026-08-07 (AR + Inventory deferred-issues session, standalone bug-fix work, no formal plan document beyond `ar_deferred_issues_plan.md`):** Dev-branch parity gaps are not limited to schema/RLS — they extend to Storage. `kadr-digital_invoice_uploads` (needed by the OCR-invoice-upload flow) existed on production but not on the dev project at all, and once created, needed its `storage.objects` RLS policies (`bucket_id`-scoped, `INSERT`/`SELECT` for `authenticated`) replicated separately from the bucket's own settings — the exact same "object exists but zero policies" landmine already logged repeatedly for tables (Phase 1, Phase 13B/13C), just on `storage.objects` instead of a data table. **Reusable check:** whenever a dev-branch bucket-not-found or storage-permission error surfaces, check both bucket existence *and* `pg_policies where schemaname='storage' and tablename='objects'` against production side-by-side — neither alone is sufficient.
- **2026-08-07 (same session):** Confirmed a reliable diagnostic for "is this import secretly still base44" that doesn't require reading the call site's error behavior at all: `@/entities/*` and `@/functions/*` imports with no corresponding real file anywhere in the repo (verified via `Glob`, e.g. `**/entities/TagAlong*` / `**/functions/searchInventory*` returning nothing) are, without exception, `@base44/vite-plugin` auto-generated virtual modules backed by the base44 SDK — meaning they always 401 through `base44-proxy` for this app's myKADR-SSO JWTs, regardless of how the call site's `try/catch` makes the symptom look (empty list, "not found," silently-swallowed error). Two more entities (`TagAlong`, and the `searchInventory` base44 *function* specifically, as distinct from the already-correctly-migrated `search_inventory_ranked` RPC used elsewhere in the same file's sibling page) were found this way. Worth running this exact `Glob` check on any `@/entities/`/`@/functions/` import before investigating further, rather than debugging the symptom first.
- **2026-08-07 (same session, testing-tool finding, extends the Phase 7 Enter-key entry above):** The Browser pane's `computer` tool's "type" action does not reliably fire a React-recognized `input` event on every controlled input — reproduced independently on two different pages' search boxes (`InventoryList.jsx` and the global `Customers.jsx` search) — even though the DOM's `.value` visibly updates. The app's underlying search/filter logic was proven fully correct in both cases only after replicating via `javascript_tool`: the native `HTMLInputElement.prototype.value` setter + a real `dispatchEvent(new Event('input', {bubbles: true}))`, followed by a real `dispatchEvent(new KeyboardEvent('keydown', {key: 'Enter', bubbles: true}))` for the Enter-triggered case. **Reusable rule:** before concluding a search/filter/form feature is broken from automated `computer`-tool typing evidence alone (zero results, unfiltered list, etc.), replicate the interaction via native event dispatch — this has now produced a false-negative bug report twice.
- **2026-08-07 (Phase 14G verification pass, real bug found and fixed):** "Replay a table's schema+RLS to production" is not the same as "the feature that writes to it works on production" — a table can be perfectly replayed while the Edge Function layer that actually writes to it silently never gets deployed there. 14C replayed `CustomerPortalStatement`'s schema/RLS to production back on 2026-08-05 (RLS confirmed correct, 1 policy, both branches), but `autopro-createStatement` — the only function that ever inserts into it, called by `StatementModal.jsx` on every open — had never been deployed to production at all (confirmed via `get_edge_function` returning `NotFoundException`). The failure was silent: the calling code's `catch` block only logs to console, so the Statement modal kept rendering normally, just with the portal-link/email flow quietly non-functional for every customer. This is the same root shape as the already-logged `autopro-createPortalSnapshot` gap from 14E, but that one was caught by accident (needed for a different task); this one was only caught because 14G's own execution plan explicitly required a consumer-regression spot-check of every newly-replayed table's consumers *outside* the owning phase's file scope — reinforcing that this specific step (easy to treat as a formality) is what actually catches this bug class. **Standing rule, new:** whenever a table gets replayed to a second environment, explicitly enumerate and re-check every Edge Function that writes to it — a `get_edge_function` 404 on the target project is the fast, definitive check, cheaper than waiting for a live click-through to surface the silent failure.
- **2026-08-07 (same session, general design lesson, not base44-specific):** A derived/matched boolean flag that's computed once and then threaded through multiple independent state-copying code paths is fragile by construction — `InventoryAdd.jsx`'s `is_existing` (drives the "New" part badge) was set only inside the OCR-invoice-import conversion block, and silently dropped by both `selectPartFromList()` (manual autocomplete selection) and `handleEditItem()` (editing an already-batched line and re-adding it), since neither of those two independently-written code paths' object-copy lists happened to include it. No error, no build failure — just a wrong badge with plausible-looking cause ("maybe the part really is new"). **Reusable pattern:** recompute a derived flag fresh at its actual point of consumption (here: `handleAddToBatch`, checked against live `inventorySearchResults` at click time) rather than carrying it as state through every path that can produce or copy the object it lives on — this class of bug will recur anywhere a similar flag exists in the remaining un-migrated surface area.